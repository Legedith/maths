# Research-paper wrap-up contract

Frozen 2026-09-10, on base ba90dd8fceb136cc757b06b9511e3aebc9b515fb.
The user's latest instruction replaces open-ended expansion with finishing a
concrete, independently audited research deliverable, pushing the repository,
and presenting the README and hosted GitHub page as a research paper.
Mapping all mathematics is not a completion claim for this wrap-up.

## Deliverables

1. A paper-style README, with abstract, precise model, theorem/proof argument,
   exact worked example, reproducibility, related work and explicit limits.
2. A readable, responsive, printable GitHub Pages version generated from the
   same paper text, with working links to the implementation and evidence.
3. Preserve the former atlas overview and all working mathematical APIs.
4. Present the implemented uniform-source mixed-target solver from PR16 as
   the computational result. Its canonical byte hash, direct first-step checks,
   symbolic identities and independent audits are authoritative; do not rerun
   unchanged local mathematical evaluations just for presentation.
5. Include the independently audited growth, fork-path classification and
   general-source infimum/attainment proofs as an analytic supplement. Explicitly
   distinguish these from the implemented solver and from Lean verification.
6. Attribute Palacios, Gomez and Del Rio's voltage/cutpoint results and the
   directed reachability optimization prior work. No unsupported priority,
   empirical speedup, real-world deployment gain or full-atlas coverage claim.
7. Retain public own proofs/reviews/typed evidence and local full source returns;
   do not publish bulk third-party full text. Keep failures and corrections.
8. Independent final four-check paper/evidence audit, deterministic PASS, then
   push the exact approved source, make it the repository front page, enable
   GitHub Pages and verify actual deployed content and current CI.

## Execution

Root alone writes the shared checkout and publishes. Subagents use GPT-6 Astra
with low reasoning effort and isolated D stages. All Python uses uv and isolated
D environments/cache. No new research direction or algorithm implementation.
The existing Sites-hosted atlas is preserved; the user's explicitly requested
publication target for this release is GitHub Pages.

Paper generation checks should verify source/content correspondence, local
links, HTML metadata and repeatable output. Hosted checks must verify the
actual response and published branch/commit. Browser interaction testing was
not requested. Final independent Critic/Resolve allows at most two rounds.

The release is complete when these delivery requirements are evidenced. This
does not assert that all mathematics has been mapped, that priority is resolved,
or that the analytic supplement is implemented or formally Lean-checked.
