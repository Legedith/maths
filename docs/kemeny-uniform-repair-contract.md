# Fixed-uniform insertion release contract

Freeze: 2026-09-08, after independent mathematical acceptance of the unit
ranking and no-op packets and independent portable-checker acceptance.
This release starts from d73319ed93131495036442b1b32b902eee18708f. Its
earlier evidence bundles describe their recorded historical revisions;
retain those bytes. This new bundle covers changed publication files.

Let G=K_(a,b,c) join K1 with integer 3<=a<=b,c. Delete uh, where u lies in
the minimum part A and h is the hub, obtaining H. The objective U is the
mean first hitting time for independent UNIFORM start and target vertices,
including equal pairs with zero hitting time. The distribution stays
fixed across designs. One missing unit-weight edge may be inserted;
restoration may be allowed or excluded. Also compare with no insertion.

Prove the exact optimum set: if a=b=c, all a-1 pairs incident to u inside
A; otherwise all internal pairs in every largest original part. Restoration
permission does not change this set. The best insertion strictly improves
U over H. Cover tied maxima, all missing-edge categories, a=3 and integer
orthant exhaustiveness. A set/orbit is not a unique edge. The no-op result
optimizes this objective under an at-most-one-edge budget, without monetary
cost or latency claims.

Use the established commute/Kirchhoff identity and rank-one Laplacian
update with correct factors. Attribute complete-multipartite inverse and
trace formulas, and generic optimal edge scoring, to primary prior work.
Retain independently audited graph equations and complete coefficient
lists. Six certificates check three ranking differences and three
exhaustive no-op cases. Pin all input bytes and dependencies; a finite
sample does not establish the universal rule.

The unchanged independently reviewed portable script reconstructs rational
score and trace expressions in sparse integer arithmetic and verifies all
six full polynomial cross-products and positive denominators. Canonical:

    uv run --project experiments/kemeny-one-deletion-proof --frozen python experiments/kemeny-one-deletion-proof/verify_uniform_repair.py --ranking-certificate experiments/kemeny-one-deletion-proof/uniform-ranking.json --noop-certificate experiments/kemeny-one-deletion-proof/uniform-noop.json --output work/kemeny-uniform-repair/check-01.json

Python 3.12.11, no package dependencies, uv, isolated D environment/cache.
Fresh output names preserve all attempts. Root may integrate the audited
code; an independent Astra agent must check final prose, sources, copied
evidence and typed claims. Maximum two final Critic/Resolve rounds; reuse
unchanged math and portable audits. Require four independent checks and
the deterministic gate, then inspect actual CI at the pushed commit.

Publish the exact (3,3,3) witness where a legal untouched-pair insertion
worsens U by 31/3150 even though resistance decreases. The weighted
threshold 1043/1322 may be described as a separately audited illustration
with an OPEN improving interval, not a weighted optimizer. Preserve the
author's earlier wording and document the reviewer clarification.

No weighted-optimum, nonuniform traffic, arbitrary graph-family, multiple
failure, multi-edge, measured application benefit, or global originality
claim. The separate later weighted-ranking candidate is excluded. The
publication contribution is a potential specific family ordering and
no-op guarantee; generic formulas and algorithmic scoring are known.
