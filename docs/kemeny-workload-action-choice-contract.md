# Workload action-choice release contract

Frozen2026-09-08. Base b9359ad7340819acc76060d91421206293cdcdbb, the completed
and independently audited workload/robustness release in PR11. Root alone
integrates the shared repository. Use D storage and uv-isolated Python. Stay
within random walks and network repair; no Site/atlas UI expansion.

## Objective and action sets

Make the choice between repairing and doing nothing explicit under the fixed
iid hub-mixture workload. H=(K_(a,b,c) joinK1)-uh has integers3<=a<=b<=c,
unit remaining conductances, u in A and hub h. The walk chooses neighbors in
proportion to conductance. Source/target are iid
p=(1-theta)Uniform+theta delta_h,0<=theta<1; self-hitting is zero. The same law
must be used for every candidate. Minimize actual expected walk steps, with
the correct baseline total conductance m and inserted total m+t.

Retain the complete previously accepted unit insertion envelope. First allow
either no action or one missing UNIT insertion. Prove strict improvement of
the optimal unit insertion for the entire equal family a=b=c>=3 and every
admitted workload. Use the exact switch margin and slope signs with a positive
polynomial certificate, not a grid. Keep all insertion and crossing ties.

The conjecture extending strict improvement to all unequal sizes is false.
For exactly(3,4,4), certify the complete action/no-action policy throughout
theta in[0,1): all internal B/C pairs below L=124/4035; their union with no
action at L; uniquely no action on(L,R0), R0=59/1662; no action/restoration at
R0; uniquely restoration above R0. The insertion-only switch14/405 is inside
that interval. All16missing edges and the baseline must be covered. Reproduce
the exact witness gap7429/11809800 expected steps at14/405. At theta1 all
actual objectives vanish, so strict conclusions do not extend to it.

Then, only for(3,4,4),theta14/405, allow no action or one missing edge of any
real strength t>0. Prove the global optimum over edge and strength, including
all candidate orbits, t=0 and the infinite-strength limit. The candidate rule
is any of the12internal B/C pairs with unique
t*=4(sqrt(27945/22432)-1) in(0,1). Verify that t=1/2 already improves over no
action by117691/11809800. Validate the fixed-workload version of the known
per-edge strength derivative, all positive denominators, the unique critical
point, endpoints, and exact radical ordering across every orbit. Numerical
approximations alone must not decide the optimum. Independent rescue review
must pass before promoting this candidate rule.

## Canonical implementation and evidence

Provide a portable pinned uv checker with Python3.12.11/SymPy1.14.0. Reconstruct
the equal-family identities/positive coefficients and the finite344 baseline
and every candidate from graph matrices or independently justified inverse
formulas. Retain exact rational quantities, polynomial coefficients, radical
isolation bounds and the actual baseline/candidate objective conventions.
Require a fresh output; keep assertions enabled if the checker uses them.
Do not evaluate executable expressions loaded from evidence JSON. Portable
entry points must not depend on D-specific historical stage paths.

The implementation must explicitly name any analytic bridges it does not
formalize, including full-domain positivity, graph reduction, envelope/ties,
strength globality and limiting endpoints. Author and implementation workers
cannot self-certify final gates. Independent reviewers must inspect exact
outputs/raw runs and implementation alignment. Use unchanged accepted
mathematical audits where appropriate, with pinned hashes and authorship.

Root canonical evaluations use the established raw logger with an explicit
uv child, fresh attempt IDs and output names. Preserve all author failures,
including the unequal universal-proof timeout and failed positivity assertion.
The final hand-derived counterexample is separate from those two machine
attempts; do not rewrite the exploratory history as a successful universal
proof. The12 frozen diagnostic rows are finite evidence, not a theorem over
unequal sizes. Preserve rejected restoration-only and fixed-uniform-choice
hypotheses.

Build typed source/method/result/conclusion provenance before final prose.
Final Critic/Resolve is at mosttwo rounds followed by the deterministic gate.
Add exact CI reproduction with raw artifacts, and inspect/download actual
post-push outputs before reporting hosted verification. This new bundle must
not retroactively change or certify PR11. Use a new branch/draft PR based on
codex/kemeny-workload-robustness.

## Contribution boundary

The established weighted commute/resistance identity, inverse edge updates,
conductance optimization and Braess-type degradation are known context.
Credit the existing source assessments; source access failures and bounded
non-identification cannot establish novelty. Keep the unresolved publisher
Kirchhoff-sensitivity and graph-join overlaps explicit. The new candidate
contribution is the precise family theorem, exact action/no-action interval
and fully optimized rescue example, with independent certificates.

No generic new inverse/optimization algorithm, physical-network measurement,
monetary intervention model, Lean formalization or completed mathematics map
is claimed. The previously audited fixed-ray threshold and finite minimax
experiments remain outside this release so the action-choice question stays
coherent. The broader objective remains active.
