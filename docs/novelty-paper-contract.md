# Conditional final paper integration

Preserve the prior publication at 59a57cdd. Prepare a private draft focused on
the audited all-r multipartite theorem and its precise implication for the
named 2019 conjecture. Publish only after the new independent bridge review,
bounded current literature comparison and independent final four-gate audit.
No further broad research grid. Retain the independent 2026 example correction
as a subsidiary result only if its canonical replay passes. Do not present a
minor source correction as the central novelty result.

Root owns shared writes. Final manuscript, renderer and evidence edits are
serial. Reuse the already independently verified immutable polynomial and
ranking checkers/certificates. One canonical local run per unchanged checker
is allowed for final integration (60s each), and one renderer build (90s),
through uv outer/child and the process-tree-aware logger. Extra local runs only
if a concrete failure/change requires them. Retain raw logs and any failure.
Provide a portable version of the accepted Fraction-only correction checker by
replacing local source/logger path metadata with a command-line output path;
preserve the mathematical calculation and compare all 89 rows to the accepted
author output. One canonical 60s integration run is allowed. Independently
review this adaptation before publication; no broader input family is added.
Final independent reviewer checks full promoted artifacts and may use one
in-memory render replay; no author self-certification. At most two final Critic
rounds. GitHub CI and actual deployed HTTP bytes are checked after authorized
push. No author contact, journal submission, or claim of peer review.
