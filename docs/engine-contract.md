# Selected engine contract (frozen before implementation)

Selected: Laplacian bridge atlas. Initial exact engine accepts connected simple undirected UNWEIGHTED graphs on 2..6 vertices. Each edge is two distinct integer vertex indices; no duplicates, self-loops, directions, weights, floats, booleans, or out-of-range indices. Source and target are distinct valid vertices. Domain is explicit; changing assumptions requires a new version.

Public Python package atlas_engine. API analyze_graph(payload) -> JSON-compatible dict with n, edges, source, target, laplacian, potentials (exact rational strings), resistance (exact rational string), hit_forward, hit_backward, commute, spanning_tree_count (integer), tree_edge_count for selected edge (integer or null), edge_probability (rational string or null), checks (mapping of named true booleans). For a non-edge source/target, resistance and commute are valid but the edge-in-tree statement is not applicable and must return null, not a fabricated probability or false identity. Every admitted instance must satisfy commute = 2*m*resistance; for an existing edge, probability = resistance. Enumerating trees is separate from the resistance implementation. Rational string convention use Fraction str e.g. 2/3, 1; zero is 0.

Use exact fractions.Fraction linear algebra for the selected resistance path. Require a separate exhaustive spanning-tree oracle using subset enumeration and graph connectivity only; it must never use the Laplacian, determinant or resistance results. For Markov first-step equations, compute hitting times independently from adjacency/transitions, without reusing a result from resistance. A small generic Fraction equation solver may be shared, with the limitation documented. Unit tests must include fixed exact results, boundaries, invalid cases, and non-edge applicability. Solve every labeled connected simple graph on 2..5 vertices in a canonical evaluator, plus named 6-vertex fixtures. Store one per-graph record in JSONL and a summary JSON; iterate deterministically with no sampling. Evaluate a selected existing edge per graph or all edges if runtime tractable; report actual counts transparently. No float tolerance is allowed for exact comparisons.

CLI: uv run python -m atlas_engine analyze PATH; uv run python -m atlas_engine evaluate --output-dir PATH. Provide schema/data validation and search only after root supplies final corpus; do not implement a fake corpus-dependent search now. Keep Python dependency-free at runtime; uv venv isolated (cache D:/CodexWorkspaces/mathematics-atlas/uv-cache). pyproject.toml may use hatchling build backend and pytest for dev. Include a basic JSON output schema and docs.

Ownership: coe_worker may write ONLY D:/CodexWorkspaces/mathematics-atlas/engine-work/{src,tests,scripts,pyproject.toml,uv.lock,README-engine.md,logs} plus its own .venv. It may read fixtures and this spec, but must not change them. No Site tools/skills, no modifications of D:/CodexWorkspaces/mathematics-atlas/project or its linked C: path. Root owns all final integration, UI, corpus, and Git. You are not alone; do not revert anyone else's edits. Record raw commands/results. Implementation worker cannot self-certify final evidence gate; independent verifier will rerun.

