# Integrate the admitted Boolean-rank connections

Root specification, 2026-09-08. Start from project commit
08434cb8f3a0923a7b781503cec64a86878cf171. This changes the curated map's ability
to carry usable mathematical translations. It does not claim discovery,
complete coverage, retrieval quality or measured real-world impact.

Use the admitted R1-R4 records in corrected-records-v1.json, SHA-256
0ba1c19b75b0b37fcd29214d3ad5565aca747481aba779084e23367b0a110fd2,
and their independent correction review, SHA-256
13bff50ecbd050f359a7948996197e9b09a12c5a5305fedb4aa40804cefcbad0.
Retain these exact bytes in evidence/boolean-rank/source-review with their
provenance. A separate integration design is advice, not a source certificate.

Add seven concepts, the four admitted relationships, four definition or
representation-boundary navigation edges, two primary sources and two learning
journeys described by the sealed integration design. Existing corpus records
remain value-identical except the schema version and scope description. The
result has 40 nodes, 52 edges, 11 sources, six journeys, six opportunities and
seven existing graph examples. Separate teaching-example and role-pilot
artifacts remain outside this release's general-claim gate.

Use schema 1.1 and explicit optional fields for source_scope,
witness_translation, local_boundary_case, notation_boundaries and
curation_record. Require them for R1-R4. Preserve the witnesses in both
directions, objective, all-zero conventions, fixed labels and bipartition,
overlap allowance, and the exact S2 nondeterministic protocol and notation
boundaries. The all-zero extensions are Atlas local definitions and elementary
proofs, not a formula attributed to a paper at log(0). Local cover load counts
vertex or row/column participation, not cell or edge multiplicity.

Render the source scope, translations, local convention/argument/result and
notation distinctions in the existing connection details. Expose the full
structured values through the Atlas API and existing connection tool. Keep
internal hashes out of ordinary learner prose. Broaden the page and coverage
copy to include the new region. The existing SVG uses computed neighbor
positions, so total corpus size alone does not require changing its bounds.

Validation must reject schema 1.0, malformed semantic fields, nonfinite or
Boolean coordinates, incomplete admitted records, malformed provenance paths
or hashes, and dangling references, while retaining structured failure reports.
Check retained provenance files against their hashes and constrain paths to
regular files inside the workspace. Verify exact admitted-record projection,
new search and journey cases, all ordered navigation pairs, the API's complete
edge fields, and rendered connection text using server rendering/HTTP only.
Do not perform browser DOM, screenshot, aesthetic or responsive visual QA.

Run appropriate Python tests, TypeScript checks, lint and production build.
Write current reports in fresh staging paths; preserve historical evidence
bytes. All executions retain exact commands, raw output, exit status and
failures. A different agent must independently compare implementation and
output against the admitted records and this contract before publication.
The author does not certify source entailment or final semantic approval.

Root alone writes shared project files and Git. Make a new codex branch.
Initialize a separate evidence bundle; prior release certificates remain
historical and cannot certify these edits. Finalize all code/docs/evidence
before the final production build and publication.
