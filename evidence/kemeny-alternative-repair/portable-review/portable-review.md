# Independent portable alternate-repair review
PASS for changed portable implementation and replay. Final release prose and assembled bundle not yet reviewed. Fixed-workload findings remain in a separate review stage and do not change this stationary-target theorem gate.

The new checker pins both alternative-repair.json and the prior optimal-repair.json by SHA256, checks new input_sha256 equals the actual pinned prior digest, and requires exactly new comparison keys B,C. It reconstructs uv,untouched_A,B,C via already-audited reduced_k, obtains the two new complete coefficient lists and prior uv-minus-untouched_A lists, checks positive coefficients/constants, and verifies each full rational identity by integer cross multiplication. All operations correctly compare final K-n values; the common n cancels. The prior expression is checked, not trusted solely because its source is labeled previously audited.

All three unchanged dependency hashes match the earlier independent portable optimal review (unchanged-check.json): verify_optimal_repair.py, verify_all_deletions.py, verify_certificate.py. Their determinant/normalization/parser/arithmetic audit was reused, not repeated. The new certificate bytes exactly match the independently accepted author's result. Prior mathematical review established both new ratios, tied-size/a=3 boundaries, and exact optimal edge set.

Fresh uv --frozen replay passed rc0 under Python3.12.11 with no dependencies and stage-local env/cache/output. replay.json is byte-identical to root canonical work/kemeny-alternative-repair/check-01.json, including certificate and all implementation hashes. A separate stage-local copy with one trailing whitespace byte was rejected rc1 with 'changed frozen alternative-repair certificate', before any output artifact was written. This expected negative test is not an unreported failure or retry. argv/stdout/stderr/runtime/rc are retained for both calls. No project file was changed.

Scoped gates: reproduction PASS (fresh exact replay identical to canonical and changed-input rejection); specification compliance PASS (unit-weight single missing edge, restoration forbidden, stationary-target K, complete B/C plus prior uv comparisons); source verification PASS (exact new/prior provenance pins, dependency hashes and prior independent mathematical certificate audit); implementation alignment PASS (three positive rational cross-products, correct provenance chain and no hidden additional comparisons). The graph counting bridge yields exactly all unordered pairs in A excluding u, binomial(a-1,2) optimal edges: one orbit, unique EDGE only when a=3. The checker states this boundary accurately rather than claiming unique edge generally.

This audit is limited to changed code/provenance/replay and reused independently accepted mathematics. Final prose and typed evidence bundle require their separately requested review. No fixed-workload optimum, novelty, publication priority, measured network benefit or multi-insertion statement is certified.
