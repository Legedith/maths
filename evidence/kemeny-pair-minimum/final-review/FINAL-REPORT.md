# Final independent Kemeny census review

Input bundle `e503ebb7cd844c14ee85efe4267393c709fc8c4a8d4e189a23f0ce1aabe28a72` passes the four scoped evidence checks. I approve claims C001-C009 and found no substantive blocker in the publication candidate.

| Check | Decision |
| --- | --- |
| implementation alignment | PASS |
| reproduction | PASS |
| source verification | PASS |
| specification compliance | PASS after recorded correction |

The exact result is minimum order 6 for finite connected simple undirected unweighted graphs under the frozen predicate `delta_e <= 0`, `delta_f <= 0`, `delta_both > 0`. The unique qualifying row through order 6 is graph6 `E?zW`, with additions `{1,2}` and `{3,4}`, Kemeny values `135/28`, `77/16`, `135/28`, `1229/252`, and changes `-1/112`, `0`, `+1/18`. Full lower-order exclusion plus this witness proves the minimum across the stated class; it does not classify graphs of order 7 or above or extend to other graph classes.

The author and blind independent implementations use different exact calculations and agree on all 1,848 stored states and 2,390 pair rows. My independent check reconstructed full labelled graph-orbit coverage, rederived every recorded pair predicate, and checked the candidate, published P7, and periodic K2 with a third exact fundamental-matrix calculation.

The original author run's author-owned wrapper violated the literal frozen ownership requirement. The retained corrective run re-pinned 42 unchanged inputs, used the previously sealed independent-auditor wrapper with the 1,800-second limit, returned zero without timeout, and reproduced all five author outputs byte-for-byte. The original deviation remains part of the record. The sole uv warning reports that an inherited independent-project `VIRTUAL_ENV` was ignored for the inner author project; it is not a mathematical failure.

The strict-singleton minimum 7 is approved only as a post-hoc secondary corollary. Every strict witness is a weak witness; the unchanged complete ledger has none through order 6, while the fixed published P7 has changes `-1/14`, `-1/14`, `+1/24`. This required no new graph evaluation and carries no novelty claim.

The portable Windows replay preserved both frozen mathematical sources and reproduced all 11 deterministic files byte-for-byte. The 33 project package files match the frozen staging package, and the existing-output probe launched zero children and preserved its sentinel. Ubuntu execution remains future CI evidence.

The cited formula, strict Braess convention, P7 example, periodic applicability, and atlas projection are source-aligned. Prior work already contains the generic Braess-set phenomenon, neutral edge additions, twin-edge formulas, and the P7 example. Bounded searches found no exact minimum-order-six statement in the inspected sources, which does not establish novelty, openness, literature completeness, or publishability. No real-world impact, prevalence, algorithmic novelty, human-time saving, or aggregate workflow-time claim is approved.

The machine-readable decisions and limitations are in `final-audit.json`. Raw checks are retained under `raw/`, including the original wrapper deviation, correction evidence, and two checker-development failures.
