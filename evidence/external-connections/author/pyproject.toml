[project]
name = "atlas-relation-import"
version = "0.1.0"
description = "Deterministic importer for the pinned MathGloss relation snapshot"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.uv]
package = false
