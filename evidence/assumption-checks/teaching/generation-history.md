# Teaching-generation history

All examples are root-authored development/teaching records, outside the blinded and real-retrieval evaluations.

- Attempt 01 used fixed literal values for the unit-weight guard. Its exact outputs are retained in `attempt-01/`.
- Attempt 02 improved the guard to inspect all off-diagonal entries of this graph's Laplacian, admitting only zero for nonedges or minus one for unit edges. Outputs are retained in `attempt-02/`.
- Attempt 03 aligned the source URL and locators with the independent verifier's retained second-edition textbook snapshot. Outputs are retained in `attempt-03/`.
- Attempt 04 reran the same final inputs on the repaired `atlas-checks/1.0.1` implementation. These are the current top-level teaching outputs. Exact numerical verdicts were unchanged by the representation repair.

Each attempt has its command and console log under the project's `logs/` directory. Earlier attempts are historical artifacts, not the current independently reviewed examples.
