# Weighted portable implementation packet

Worker implementation complete; independent implementation/reproduction review required. No self-certified evidence gate. Nine full exact identities and two finite rational diagnostics succeed in canonical attempt2, rc0, empty stderr. Attempt1 produced matching output but its PowerShell exit-code capture was null; original artifacts and corrective plan retained. Two attempts total, each bounded90seconds; no implementation changes between them.

Canonical command, replacing stage with destination upon unchanged integration:

    uv run --project D:/CodexWorkspaces/mathematics-atlas/weighted-portable-work --frozen python D:/CodexWorkspaces/mathematics-atlas/weighted-portable-work/verify_weighted_repair.py --weighted-certificate D:/CodexWorkspaces/mathematics-atlas/weighted-portable-work/uniform-weighted.json --noop-certificate D:/CodexWorkspaces/mathematics-atlas/weighted-portable-work/uniform-noop.json --output D:/CodexWorkspaces/mathematics-atlas/weighted-portable-work/check-02.json

Use a fresh output path for another replay. Environment: UV_CACHE_DIR=D:/uv-cache, UV_PYTHON_INSTALL_DIR=D:/uv-python, UV_PROJECT_ENVIRONMENT=stage/.venv; copied project lock and Python3.12.11 pin; no dependencies added.

New script imports unchanged audited Poly/positive, coefficient parsing indirectly through verify_ratio, and graph_expressions trace/m/unit scores. Three dependency hashes are enforced at runtime and emitted, with its own hash and both certificate hashes. All used polynomial variables are x,y,z; explicit fourth-exponent-zero checks guard reconstructed r/s and finite evaluation. The arithmetic's fourth-dimension truncation cannot affect these computations, including imported expressions, which also use only x,y,z.

Confirmed simplifications from accepted formulas: rho/k=(n+d-1)/k0, W/k^2=w0/k0^2. For uv, r=(2k0+n)/(d*k0); multiplying the accepted squared norm by d^2*k0^2 gives 2k0^2+2n*k0+w0. Untouched r=2/(n-q),s=2/(n-q)^2. Multiplication and difference of rational pairs clear denominators without division or certificate expression-string execution. Imported no-op expressions are unchanged from the previously audited unit checker.

Finite diagnostics are explicitly labelled finite: crossing12 at (4,4,6), and radical prefactor189/59/radicand1573160/1037853 at (3,3,3), with exact rational bounds0<t*<1. Generic derivative, weighted graph bridge, exhaustive orbit/domain proof, and global joint optimum remain accompanying independent proof obligations. No weighted performance/novelty or monetary optimum claim.

Only new stage was written. Existing project, helper sources and proof packets unchanged. hashes.json records exact copies and output artifacts for independent reviewer. No mutation rejection test was consumed by the worker; reviewer may run it independently.
