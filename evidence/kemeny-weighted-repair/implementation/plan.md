Implementation plan frozen before execution. H1 reconstructed r/s rational pairs produce all six pinned weighted coefficient identities exactly with positive denominators/constants. H2 imported unchanged trace/m and scores produce all three pinned no-op identities. H3 exact Fraction finite diagnostics recover crossing12 and the (3,3,3) radical with strict0<t*<1. H4 no fourth polynomial variable is used; dependency and certificate bytes are pinned and emitted. At most two implementation attempts, each capped90seconds, uv frozen Python3.12.11 stage environment on D. No new dependencies or project writes. Independent final implementation reviewer required; worker does not self-certify.
