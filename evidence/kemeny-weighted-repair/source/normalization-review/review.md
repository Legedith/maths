# Independent source and normalization review

Reviewer /root, distinct from source investigator /root/astra_damage_symbolic.
PASS for the scoped attribution and algebra, not publication originality.

I read the primary author-PDF return retained in
astra-strength-priority-work/raw-follow.json. The Ghosh-Boyd-Saberi paper's
printed page38, equation2 (returned lines69-74), minimizes total effective
resistance with nonnegative conductances summing to one. Lines58-62 relate
this objective to mean commute/hitting time. Those passages support the
stated known-framework attribution. They do not establish that the exact
single-edge radical or the damaged-family ordering was previously stated.
A fresh direct open of the same author PDF timed out; this review uses the
retained primary return, not a claim of fresh successful retrieval.

The normalization is an independent mathematical inference. Common scaling
by 1/(m+t) scales adjacency and degrees equally, so the transition matrix
is unchanged. Put alpha=t/(m+t): each old unit weight becomes
(1-alpha)/m and the inserted weight becomes alpha, an affine line in the
unit simplex. For finite t>=0, alpha<1 and the connected old graph retains
positive weights. Pseudoinverse scaling gives normalized Kirchhoff index
n(m+t)trace((L+t vv')^+); by the already independently checked weighted
commute identity, U is 2/n^2 times this index. Therefore the target is a
restricted one-dimensional slice of the known conductance-allocation model.
Its fixed old-weight ratios are an additional restriction; the family
optimum is not a claim to optimize all existing conductances independently.

The root notes and final proof should credit this overlap. Differentiation
of the rational update is elementary; no generic new algorithm is claimed.
The source investigator's four-query non-identification remains bounded.
Coordinate-restricted literature and earlier inaccessible fulltexts remain
gaps. Source application discussions do not prove impact for this design.

All seven frozen input hashes are checked by inspect_inputs.py with retained
raw invocation/output. This is an artifact-integrity check, not a numerical
experiment or automated substitute for the source reading above. Bulk
primary returns remain local; the public release may copy this review,
the concise source notes, verdict and input hashes without those returns.
