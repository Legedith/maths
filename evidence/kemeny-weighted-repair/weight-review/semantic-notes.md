# Semantic audit

Reuse the previously accepted graph inverse and orbit coverage. Restoration has r=rho/(1-rho); incident-u A pair has r=2/d+1/(d^2k). Independently grounded unit scores already give S=s/(1+r), so reconstructing s=S(1+r) avoids transcribing the author's squared-norm expressions. Untouched contrasts have r=2/(n-q),s=2/(n-q)^2. All denominators positive on connected H. This domain includes a=3; no new residual cell assumption is introduced.

For a common prescribed t>0 every graph has total conductance m+t. Conductance-proportional discrete random walks and independent uniform source/target, with zero self-hit, therefore compare by the same positive prefactor 2(m+t)/n and the trace reduction t*s/(1+t*r). Dropping the common positive t gives the proposed score. Its pairwise numerator is exactly intercept+t*slope. Positive constants in both shifted components imply strictness throughout the nonnegative parameter orthant, including t approaching zero from above. At t=0 actual interventions all coincide with H and tie; the candidate explicitly excludes t=0. No uniform positive lower improvement bound in U as t tends to zero is asserted.

For untouched parts let d=n-q>0. Denominator d(d+2t) strictly increases with d because its derivative 2d+2t>0. Thus their score strictly increases with q. The same three comparisons as the accepted unit proof suffice for the exact maximum set. First shift covers all a>=3,b,c>=a. Third shift covers b>=a+1,c>=a, using integral sizes; exchanging B,C covers c>a. Equal largest parts tie exactly; restoration is always strictly dominated by uv. At all equal, uv beats every other orbit; elsewhere a largest part beats uv and therefore restoration. No uniqueness of individual edge follows: all edges within winning orbits attain the optimum.

Prior source verification is reused from astra-fixed-workload-source-work/report.md and astra-uniform-noop-review-work/review.md: Chandra Theorem2.2 with unit step cost gives volume 2(m+t), and Monnig–Meyer gives the known single-edge electrical update. This is fixed-weight location comparison only, not weight selection, no-op optimality, physical improvement or novelty. It does not imply all suboptimal pairs maintain their order.
