# Frozen forbidden-restoration exploration

Conjecture: C-I-lambda_I>=0 for every integer3<=a<=b<=c with c>a, using unit insertion. H1 strict positivity holds under two exhaustive unequal orthants b=a,c=a+1+z and b=a+1+y,c=b+z. H2 exact rational numerator certificates can establish the endpoint gap, hence no incident crossing for theta<1. H3 only largest-part internal pairs win, with B/C ties if b=c; no-action and strength choices are excluded.

Approaches: first cancel using n,d,e then substitute the two integer orthants and seek full nonnegative coefficients with positive constant. Second, if this fails, inspect factored endpoint inequality in d-e rather than infer a counterexample from negative coefficients. At most two60second logged symbolic evaluators; no grid, no shared writes. Reuse accepted unit/common-strength inverse and orbit formulas. Author proposal only, independent review required.
