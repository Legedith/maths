# Independent analytic audit

All four scoped gates PASS. No correction required. Target proposal SHA256f4fc5119ca0f35badbb7c2b31f90a574d86a12459f03f5d0251c8b1ab2d59f75. This reviewer did not author the target. Analytic review only: no numerical evaluator, graph expansion or source query. Shared files remain unchanged; excluded from PR12.

Reproduction PASS by exact analytic verification. For positive affine numerator A+Btheta and denominator C+Dtheta, the quotient derivative is(BC-AD)/(C+Dtheta)^2. It has constant sign. Every ratio therefore lies between its endpoint values, including constant ratios. Since L_e/min_j L_j=max_j(L_e/L_j), its maximum on the interval is exactly the larger endpoint maximum. Finite nonempty action set and positive continuous lines ensure both oracle and all maxima exist. This proves the endpoint theorem even across any number of oracle changes; l=h is immediate. No numerical reproduction is needed for this identity and inequality proof.

Specification compliance PASS. The graph reduction assumes the same vertices, fixed connected positive-conductance graphs and the same iid hub-mixture law across actions, zero self-hitting, and0<=l<=h<1. Each action's fixed volume is absorbed in its own line; only common1-theta cancels. n>=2 implies trace(M)>0, so the graph-derived lines are positive. The broader abstract theorem merely requires positivity on the interval, not positive slopes. Theta1, point masses yielding zero objective, disconnected/infinite objectives, workload-adaptive choices and infinite or randomized action families are excluded. No multipartite domination or graph realizability of abstract examples is inferred.

Implementation/argument alignment PASS. In the strict interior two-line crossing orientation A(l)<B(l), B(h)<A(h), A's sole positive endpoint regret is A(h)/B(h)-1 and B's is B(l)/A(l)-1. Comparing them gives A(l)A(h)<=B(l)B(h), with the direction as printed. Strict inequality selects one action, equality both. Strict monotonicity of the relevant ratio gives the stated unique worst endpoint for each. Boundary crossings give the interior-smaller line zero regret everywhere and its competitor positive regret at the other endpoint. Coincident lines and collapsed intervals are correctly treated; physical action labels sharing a line are retained.

Domination reasoning is precise. A fixed retained action weakly below an excluded line everywhere implies its regret is no greater everywhere, while preserving the oracle; hence deletion preserves value and at least one minimizer, but can erase tied minimizers. Strict domination at both endpoints ensures strict endpoint regret inequalities. At an endpoint attaining the retained action's maximum regret, the excluded action is strictly worse; therefore its worst regret is strictly larger. This justifies the sufficient strict-elimination condition.

Both abstract examples check exactly on[0,1/2]. A and B have endpoint vectors(1,2) and(2,1), oracle endpoints(1,1), and worst regret1. For C=3/2+theta, endpoints(3/2,2) give regrets(1/2,1), and C-A=1/2-theta>=0, so a dominated action still ties the minimax value1. For C=3/2, regrets are(1/2,1/2), while min(A,B)<=3/2 everywhere (equality at1/4). Thus oracle-inactive C strictly improves minimax value to1/2. These affine examples verify the claimed elimination failures without any graph realization claim.

Source verification PASS, scoped reuse. The accepted weighted commute/covariance bridge gives U_e=(1-theta)*2m_e[trace(M_e)/n+theta(M_e)_hub,hub]. The proposal neither cancels unequal volumes nor treats stationary-target weights as fixed iid demand. Elementary quotient calculus and finite maxima supply the new analytic consequence. No external priority citation or generic algorithm novelty is claimed; no fresh source search is needed. The accepted common-strength/workload review is pinned in input-hashes.json as the unchanged bridge, not as a graph-family minimax theorem.

Supported scope: endpoint worst-relative-regret reduction for a finite positive affine reduced family; complete deterministic minimax selection over the full action set; conditional two-action product criterion; precise weak/strict domination distinctions and both counterexamples. No graph-family action elimination, general demand law, physical benefit or publication clearance is established.
