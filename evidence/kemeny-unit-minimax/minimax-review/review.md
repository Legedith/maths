# Independent full unit minimax audit

All four scoped gates PASS. No correction required. Target proposal SHA256 fc9f481397a4979d3f56093eb4ff7d104ff3d086db65cad8418ce4c11c3eae6e and result SHA256 2c3081854bbed71c05770583d117b1ae03aed6646ff1de19ca3c07e299d3abca match the actual bytes. This reviewer did not author the target. No shared files, Git state or PR12 were changed.

## Reproduction

One independent logged standard-library Fraction evaluator returned 0 in 0.406 seconds under a 60-second cap through uv Python 3.12.11 with D cache/environment paths. No retry, second evaluator or expanded grid. check.py independently builds all ten missing-unit-edge candidate Laplacians on 333 (35 old edges), inverts each full L+J/n by Gauss-Jordan, subtracts J/n and verifies every entry of LM=I-J/n. It does not execute the author's score formulas or solver. result.json retains every reduced line and both endpoint regrets. All target and inherited manifest hashes match.

The reconstruction gives incident line 10021/8680+(2547/2480)theta and restoration line 81/70+(9/10)theta. At interval [0,21/500], the crossing is 46/2205, below midpoint 21/1000. Restoration uniquely minimizes at the midpoint. Exactly edges (0,1),(0,2) minimize worst relative regret, each 23305/10372104, strictly below restoration's 23/10021. Every other candidate's endpoint regret was computed and is strictly worse. Both reported exact endpoint products and midpoint values match. The general endpoint proof, not sampling, makes these endpoint maxima full-interval maxima.

## Specification and full-domain argument

The domain is precisely integer 3<=a<=b<=c, required one unit missing-edge insertion, restoration allowed and no action excluded, deterministic action selected before theta in a closed [l,h] subset [0,1). All final volumes are m+1; hence the common 2(m+1)(1-theta)/n cancels in relative regret. It would not cancel against no action. For connected candidates, trace(M)>0 and M_hh>=0, so the reduced lines trace(M)+n theta M_hh are positive on this domain. No slope assumption beyond positive denominators is needed.

The distinct forbidden-restoration review proves C-I-lambda_I>0 for both exhaustive integer unequal cases b=a<c and a<b<=c, including b=c. Thus each largest-part action strictly beats each incident-u action for all theta<1. The score 2/[(n-q)(n-q+2)] is strictly increasing in q, so it strictly beats every nonlargest untouched action too. Equal-family incident-u strict intercept dominance and positive workload coefficient exclude all untouched actions; the accepted common-strength review supplies this ordering and the unique restoration crossing. These are fixed-action strict dominations. The surviving action set X has a-1 edges if all sizes equal, binomial(c,2) edges if c>b, and 2*binomial(c,2) if b=c>a. Restoration is one additional edge. At a=3 all relevant classes still exist. No omitted action can tie minimax: at an endpoint attaining X's maximum regret, its strict dominator comparison gives the omitted action a strictly larger regret and therefore a strictly larger maximum.

For completeness, L_e/min_j L_j=max_j(L_e/L_j). Each positive affine ratio has derivative with constant numerator divided by a positive square, and is monotone or constant. Every action's worst regret is therefore attained at an endpoint. Strict crossing A(l)<B(l), B(h)<A(h) makes A/B strictly increasing, so X's unique worst point is h and restoration's is l. Comparing A(h)/B(h)-1 with B(l)/A(l)-1 and multiplying positive A(l)B(h) yields A(l)A(h) <= B(l)B(h), with exactly the direction printed. Equality retains both complete physical action sets; strict inequalities select only the stated set.

All boundary branches are correct. When l=h, the oracle choices and crossing union have zero regret; strict domination still excludes others. For l<h and h<=tau, X alone is oracle everywhere and minimax, even when h=tau; restoration incurs positive regret at l. The reversed argument applies when l>=tau, including l=tau. If unequal tau>=1, every admitted interval lies strictly below it. Equal tau is strictly between 0 and 1. In an interior crossing, product equality keeps the full union, including both largest parts when b=c. Within a winning one-sided orbit every workload attains its zero regret. Theta=1 yields zero actual objectives and undefined relative regret 0/0, so its exclusion is necessary. There is no coincident-line case: accepted positive slope difference and a unique formal crossing rule it out.

## Source and implementation alignment

Pinned distinct dependency reviews were read: forbidden-restoration review SHA256 652799067c254044bd262961814be5ff62b9a13237d5049908119c05d07e5e74, endpoint review c0b0112767bb251a87c45eefccc12dd803f69ca7a0ca1e61f7774a7899a274d8, and common-strength review edaf10d12ad3de86844b22697c1aae06ce662fb5c59e2b410e2b302c009d7671. Their proofs supply strict unit domination, the covariance/inverse bridge and positive-affine endpoint reduction. Their accepted statements entail the uses here. No fresh retrieval or generic-method novelty clearance is needed or asserted. This source gate is scoped reuse, not an exhaustive priority search.

The author's single Fraction diagnostic and retained rc0/empty stderr agree with its description. It checks only the predeclared 333 interval; the theorem is explicitly analytic and is not inferred from the diagnostic. Our evaluator likewise remains diagnostic, with the complete family proof audited above. Author and independent raw scripts/streams/status are retained, not silently replaced. No failures occurred in this audit. No midpoint heuristic theorem, randomization, variable strength, no-action optimization, arbitrary workload law, physical benefit or public portable implementation is certified.

Gate conclusion: reproduction PASS; specification_compliance PASS; source_verification PASS by scoped distinct dependency reuse; implementation_alignment PASS for this analytic research packet and its sole diagnostic. PR12 and subsequent publication gates remain separate.
