# Independent robustness integration review

PASS, separately scoped to the finite adversary packet and the small CLI change. No material correction. No shared/project writes or publication approval.

## Finite adversary: four scoped gates

Reproduction PASS. Batch1 used independent grounded post-insertion inverses to reconstruct the restore/runner gradient values in all eight nominal contexts, then checked all16 recorded sign patterns, every old-edge weight, every candidate coverage/set/flag/gap and summary. All16 targeted margins decrease; there are zero outside failures at1% and exactly three at10%, all theta1/10, on334,345,446. The first334 witness's entire13candidate objective vector was independently reconstructed directly from its recorded exact weights, one grounded inverse per inserted candidate. Urestore=8286027872520913257/908367355347833150 and Uwinner=29313964633299551/3218584043663450 agree; unique winner is edge(6,7). Other perturbed objective vectors were checked for exact bookkeeping, not independently recomputed. One4.82s batch, rc0, empty stderr,60s timeout.

Specification compliance PASS. Exactly four graphs times two theta values times two epsilon values=16 patterns. Existing edges are all original cross-part/hub edges except uh, each appears once, and every recorded weight equals1-epsilon*sign(recorded gradient), hence lies in the box[1-epsilon,1+epsilon] and remains positive. Each nominal runner is the smallest exact(Q,edge) outsider, with restoration uniquely nominal-best. Missing edges are all within-part pairs plus uh; after perturbation all actual edge identities are retained. These deterministic feasible assignments establish three counterexamples to universal containment for those10% boxes. The zero1% failures are not a robustness theorem.

Source verification PASS, scoped reuse. The separate independent source review ../astra-link-robust-bound-review-work/source-review.md was read. It supports known inverse/resistance sensitivity machinery and distinguishes neighboring consensus/continuous-weight objectives from this discrete insertion problem. No new priority conclusion, worst-adversary optimality or physical impact is asserted. I do not use my current author theorem as an independent source gate.

Implementation alignment PASS. For Q_e=tr(M_e C), differentiating an old conductance yields -tr(M_e f f^T M_e C)=-(M_e f)^T C(M_e f); subtracting restore from runner gives the recorded gradient sign. A componentwise opposite sign minimizes the linearized Q-gap over the box but need not minimize the nonlinear gap. The actual U includes common positive2(perturbed_old_volume+1), with volume verified from every recorded weight. The code does not claim the gradient direction is U-optimal or worst-case. The first_counterexample object equals the first failing row, and all target-gap/optimizer flags match exact stored values. This does not conflict with single-old-edge robustness because the feasible sets differ.

## Portable CLI: four scoped gates

Reproduction PASS. Batch2 ran a byte-identical owned copy of the proposed script, with --output pointing to a fresh owned path. Python3.12.11, uv isolated/D cache,1.92s, rc0, empty stderr,60s timeout. Every decoded JSON field equals the accepted root result after removing ONLY implementation_sha256; the embedded new hash matches the actual copied script. No mathematical field, record, minimum, count, metadata or coefficient differs. cli-comparison.json and raw streams retain evidence.

Specification compliance PASS. Only argparse import, required --output parsing and destination selection changed. The exact recomputed unified diff matches changes.diff (the task's 'exact-changes.diff' name was a read-only filename miss; actual artifact is changes.diff). No graph/workload/interval or proof criterion changed. The parent output directory must exist, as documented. This is local implementation review, not repository integration or a publication gate.

Source verification PASS, scoped reuse. Damage's independent ../astra-continuous-perturb-review-work/review.md is reused for the accepted mathematical prototype c6ca55ac065e8ed75ff16680dfddea2a3d32e4e0054fe2ee7836bfe2bbe83625. It independently reconstructs all3896 comparisons over96continuous contexts and pins a distinct source reviewer. This audit does not re-prove those certificates or use an author self-verdict.

Implementation alignment PASS. Exact diff inspection establishes unchanged mathematics. The output guard remains immediately before write and refuses existing outputs during normal execution, after computation as documented. Author attempt2-existing returned1 with the stated guard and retained output bytes; its raw failure metadata/stream and comparison record were inspected. No extra negative case was run. The guard is an assert, so invocation under Python -O would disable it; the documented and tested CLI invocation uses ordinary Python without optimization. This review makes no stronger concurrency/atomic-write or optimized-interpreter guarantee.

## Frozen provenance and limits

Adversary report SHA2563a62abc44630dfcd56c206ba6e0e517af7d7ccac25c383abd90b48bacc2a900a; result79e26d6215ab39316658a6480440c2ccc005fbc9bb9df7435628c7c2c91f2171. CLI script6625373266410d302527d509516bb7df6c9a397573f42ba85798d9fe089853bb; accepted root result13d3f0af59ccae1e848fe2c1c79d87cca1539b3e304fc6c8d3994ab0af5b09e0. These were verified before their runs. input-hashes.json pins inspected code/reports/source reviews and evidence. Two logged numerical executions total, no expanded grid or retry. New files use explicit UTF-8/ASCII; all author and shared stages remained unchanged.

This acknowledgment covers finite adversary correctness and the stated CLI transformation only. It does not certify a worst-case perturbation threshold, generic-method novelty, current authored common-strength theorem, measured impact, final integrated public artifact, or new release.
