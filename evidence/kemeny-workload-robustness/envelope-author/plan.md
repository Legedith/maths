# Frozen analytic plan
H1: stated common-t score/slope formulas follow from accepted inverse actions and unit formulas. H2: unequal E_t has the proposed strictly positive lower bound, so only largest-part edges and restoration can win. H3: both equal and unequal workload switching thresholds strictly decrease with t>0; equal threshold always lies in(0,1). Check all algebra, endpoints and limits without assuming positivity of a limit. One symbolic batch <=60s, no parameter grid; optional second bounded diagnostic only333/334 at t1/2,1,2 if an unresolved identity needs it. Reuse accepted uniform common-t theorem and workload covariance bridge. No joint-strength optimum, universal integer endpoint classification, novelty or physical impact. Explicit UTF8/ASCII; owned stage only.
