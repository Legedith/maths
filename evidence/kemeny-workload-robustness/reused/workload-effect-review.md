# Independent finite effect-size review

PASS, all four checks, 2026-09-08. Reviewer /root/astra_damage_symbolic. No material correction. This is an eight-instance modeled-objective audit, not a public implementation gate or physical-impact assessment.

## Reproduction

One frozen <=60s uv Python3.12.11 rational evaluator passed rc0 with empty stderr. It did not run the author's grounded code or recompute a matrix/grid. It reused the independently accepted finite orbit intercepts/slopes in astra-workload-screen-work/result.json and checked every missing edge via its orbit. All eight exact excess fractions and all eight exact baseline-reduction fractions match. All recorded baseline, insertion, uniform-choice and minimum values match independently evaluated rational formulas.

For d=n-a, k=(n-1)(d-1)/(nd), W=[n(n-1)+d(d-1)]/(n^2 d^2), I used T=sum_q (q-1)/(n-q)+3/n+W/k and M_hh=(n-1)/n^2+1/(n^2 k). These follow from the accepted intact complete-multipartite eigenspaces and rank-one deletion: intact trace is the first two terms, intact hub diagonal is(n-1)/n^2, deleted-edge image has squared norm W and hub coordinate -1/n. Thus no author's effect-size output was used to set baseline values.

Baseline is2m(1-theta)(T/n+theta M_hh); insertion is2(m+1)(1-theta)(T/n+theta M_hh-alpha-theta beta), with accepted alpha,beta. The changing volume is retained. The excess denominator is the positive workload optimum; the improvement denominator is the positive no-action baseline. Neither is the uniform-choice objective.

## Specification compliance

Exactly333,334,345,446 at theta1/10 and1/2, matching the frozen plan, with no additional instances. Every missing edge is covered, including restoration:10,13,20,28 per graph. Uniform-optimal sets are respectively {(0,1),(0,2)}, all6 C pairs, all10 C pairs, and all15 C pairs. The evaluator obtains these sets independently by minimizing the theta0 formulas, checks every recorded edge and its equality, and verifies unique restoration at both positive workload values for every graph. All eight best repairs strictly beat baseline, only a finite conclusion.

## Source verification

PASS for reuse of accepted covariance/commute, finite envelopes and intact/deletion identities. Prior independent packets astra-workload-screen-review-work and astra-workload-family-review-work supply the accepted mathematical inputs. The earlier independent two-edge source audit of source-notes-v2 and Ghosh fulltext credit is reused; no new priority claims or source retrieval. The generic electrical/workload method remains known, and effect percentages are not evidence of deployed benefit.

## Implementation alignment and execution history

PASS. Inspected check2.py builds the stated H, centers the grounded inverse, evaluates all missing edges and keeps every original uniform optimum. Its rank-one trace and hub-coordinate correction agree with accepted formulas. The replay verifies the supplied report SHA d2316235e21ae7f05154bb8d2acf645e563e966e6588a14aaba17515ca1cd9cb and result SHA f7de3722c44e98b2205f0aadc48329640d9688a79c081d3176c95fce07faa64e, plus every author manifest entry.

The first author attempt is a real rc1 failure at JSON serialization of BooleanTrue. amendment.md explicitly authorizes one serialization-only recovery. The evaluator confirms the scripts differ solely by wrapping best<base in bool, retains the first traceback/rc1, and confirms recovery rc0/empty stderr. These are not two ordinary successful batches. This reviewer performed exactly one successful evaluator, with argv/runtime/rc and byte streams retained.

## Percentage interpretation

Uniform-choice excess ranges0.10814670696% to3.78673835125%, correctly summarized approximately0.11%-3.79%. Baseline reductions range0.24539223694% to3.15295367992%, correctly summarized approximately0.25%-3.15%. The rounded0.25% lower endpoint is not an exact lower bound. Raw per-row percentages and exact fractions are in stdout.bin. The report says approximately and limits the result to modeled expected steps, so the wording is supported. No traffic realism, wall-clock speedup, cost-adjusted usefulness, universal no-action improvement, or universal location conclusion follows. theta1 would be all self-traffic and is not included.

## Evidence

plan.md, check.py, run.py, run.json, stdout.bin, stderr.bin retain the sole review execution. input-hashes.json pins reused inputs and original failed/recovery evidence; hashes.json seals this packet. Shared project and all author/prior stages remained read-only.
