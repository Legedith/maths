# Continuous one-link perturbation certificate

Frozen 2026-09-08 after the previous goal turn made verified progress: a full-family workload-dependent repair theorem, exact counterexamples and independently audited model effect sizes. Public project remains at PR10; no unchanged release reruns. Root owns this stage; agents have distinct read-only research/review responsibilities. Python uses uv, an isolated D-drive environment/cache, pinned Python3.12.11. New text is ASCII or explicit UTF-8. Old failures and historical byte encodings remain intact.

The current endpoint screen found no optimizer outside the original optimizer set on four graphs, three workloads, eight existing-edge orbits and two perturbation endpoints. That finding alone does not establish interval robustness. This new declared phase freezes exactly the same four graphs333,334,345,446, theta0,1/10,1/2, and old-edge representatives u-B,u-C,Aother-B,Aother-C,B-C,h-Aother,h-B,h-C. It covers the full real interval delta in[-1/10,1/10] for ONE changed existing edge of conductance1+delta. All other old weights and the one inserted missing-edge weight stay1.

For each nominal graph/workload, compute the complete original optimizer set O exactly. Test the stronger sufficient condition that EVERY nominal optimal edge remains strictly better than EVERY nominal nonoptimal edge over the whole interval. Ties within O may break; equality of the full old/new optimizer sets is not asserted. Original graph/workload symmetries justify old-edge representatives, but after perturbation every actual missing-edge candidate is evaluated. The result is a finite graph/workload classification with a continuous interval, not arbitrary graph sizes or simultaneous changes.

H1: the quadratic comparison identity correctly represents every candidate-pair objective comparison while its denominators stay positive. H2: every optimal-versus-nonoptimal quadratic has a positive minimum on the complete interval. H3: the computed original sets and representative old edges cover the frozen model without assuming symmetry survives the perturbation. If H2 fails, retain its exact rational minimizing argument and distinguish failure of this stronger condition from failure of optimizer-set membership.

Use a fresh Fraction grounded inverse for each baseline, and rank-one updates for candidate insertion and existing-edge perturbation. Every quadratic minimum is checked at both endpoints and, if convex with an interior vertex, at that vertex. Prove the generic identities and interval-minimum argument analytically; positive endpoint samples alone are not the criterion. All computations, coefficients, minima, sets and execution streams are retained.

At most two <=60second numerical batches: one initial certificate construction, then only a same-domain diagnostic recovery if needed, with its reason recorded before execution. No added graph/workload/perturbation cases. Independent code/semantic/reproduction review is required before promotion; root cannot certify its own implementation. Generic inverse updates, quadratic comparisons and Loewner bounds are known methods. No generic-method novelty, physical effect, monetary cost, no-action or full mathematics-map claim.
