# Independent simultaneous-link bound review

Mathematical packet: all four scoped gates PASS. Source-note assessment: PASS within explicitly limited primary-access scope, separately detailed in source-review.md. No public-release approval or continuous single-edge interval review is given.

Frozen proposal SHA256 fffdd5e73582bf7ed4b396b47f4433f7e139891302a17cd189d90313a30a73e5 verified. Accepted effect input SHA256 f7de3722c44e98b2205f0aadc48329640d9688a79c081d3176c95fce07faa64e verified. Independent reviewer did not author this bound/source packet. One Fraction/hash batch, rc0, empty stderr, about0.4seconds, Python3.12.11 via uv/D, no new matrices/grid/search. Raw argv/streams/rc and result retained, text explicitly UTF-8/ASCII.

- Reproduction PASS: check.py independently sorts every exact accepted missing-edge objective into distinct levels, obtains complete minimizer and nearest-outsider sets, and computes radius=(ratio-1)/(ratio+1), ratio=outside/best. All eight fractions and sets agree with author output. Exactly three radii strictly exceed1/100; five are smaller. This is reused-data reproduction, not a new graph simulation.
- Specification compliance PASS: perturbation vector is shared across candidate comparisons, applies simultaneously to every existing weight, and leaves each inserted edge unit. Workload p is fixed, iid and independent of weights/candidate. Final volume2(m_delta+1) is common and cancels. Comparing against no action is outside scope. At epsilon strictly below radius all old winners beat all outsiders; old internal ties may break. Equality at radius gives non-strict separation only and may allow outsiders to tie. Epsilon1 is excluded. No-outsider and point-mass cases are correctly vacuous.
- Source verification PASS for known-method boundary only: accepted iid commute bridge supplies the trace objective; inverse-order and PSD-trace inequalities are justified analytically below. The separate source report correctly credits established perturbation/optimization methods and keeps consensus stability distinct from discrete argmin stability. No novelty or actual failure threshold follows.
- Implementation alignment PASS: author check.py uses every accepted missing-edge value and strict value levels to define the complete old optimal set and outsiders. Radius flags distinguish >=1percent from strict certification. No perturbation realization is manufactured from a small radius. The table and raw results align. The uniform-scaling nonsharpness argument uses the accepted common-positive-weight theorem only for uniform workload, as explicitly stated.

Independent proof: each old-edge Laplacian w bb^T is PSD. Summing its scalar bounds gives (1-eps)L_H<=L_Hdelta<=(1+eps)L_H. Adding unchanged B_e yields lower slack eps B_e and upper slack eps B_e relative to (1+/-eps)L_e, so the stated bounds hold for every candidate, including restoration. Connected positive baseline and eps<1 keep a common nullspace span1. Restricting to1-perp permits inverse order, then extending by0 yields the pseudoinverse inequality. For PSD C, trace(CX)>=0 whenever X>=0, since it equals trace(C^(1/2) X C^(1/2)). If p has at least two positive entries, C is nonzero, C1=0, and positive-definite inverse on1-perp makes each trace strictly positive even if C is rank deficient. A finite complete optimizer set has a strict positive outsider gap, giving0<radius<1. Solving best/(1-eps)<outside/(1+eps) gives exactly the displayed sufficient threshold. This equivalence concerns the bounding comparison, not necessity for actual stability.

No-outsider case: O is the entire fixed candidate universe, hence containment is automatic. Point-mass p has C=0 and all objectives0. At theta<1 the hub mixture has full support; theta1 is the point-mass degeneration. No undefined0/0 is needed.

Uniform-scaling counterargument is valid but scoped: scaling every old weight by q>0 and retaining new weight1 is transition-equivalent to unit old weights and insertion1/q. For uniform iid workload the accepted optimum set is invariant in positive insertion weight. Thus this path can remain stable beyond the sufficient radius, refuting a general sharp-uniform-scaling interpretation. It does not establish the worst-case simultaneous radius, nor preservation for each positive-theta instance.

No correction required to the bound. The apparent contrast with the earlier10percent screen is expected: a conservative simultaneous-all-links certificate can be much smaller than tested single-link robustness. Those are different quantifiers.
