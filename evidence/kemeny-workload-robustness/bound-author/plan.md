# Frozen plan

Domain: connected H=(K_a,b,c join K1)-uh, 3<=a<=b<=c, fixed iid endpoint law p, zero diagonal hitting, exactly one unit missing insertion. Every existing edge weight changes simultaneously by arbitrary relative delta_f with |delta_f|<=epsilon<1, identically across candidate comparisons. Candidate new edge remains unit.
H1: the strict gap radius (Qoutside-Qstar)/(Qoutside+Qstar) guarantees every old optimum beats every outsider. Prove by Loewner/inverse order; handle no outsiders, degenerate p, ties and endpoint equality explicitly.
H2: this radius is at least1/100 in each of the eight accepted effect instances. One exact Fraction batch only using all recorded candidate objectives; no matrix or new grid. Verify source result and independent effect-review hashes first; retain all failures of H2.
H3: this sufficient bound is a sharp uniform-scaling threshold. Examine analytically only, using accepted common-positive-strength uniform theorem if applicable; no scaling sweep.
At most2 logged batches <=60s; second only a diagnosed implementation recovery with retained failure. uv isolated and D cache. New files explicit UTF8/ASCII; all prior/shared stages read-only. Three further hypotheses proposed but unexecuted. Generic perturbation machinery known; no novelty, physical impact or self-certified audit.
