# Independent directed-workload portable audit

Four scoped gates PASS. No required correction. Auditor /root/astra_ranking_verifier authored neither this implementation nor the directed theorem. No public release, hosted CI, priority or application approval.

## Reproduction: PASS

Frozen solver a6e5c9fcfead2c952b1a4d1b66b967fd6e6613452cccb7137ce75e80c343ede2; checker737ae7a365a2ac1c6a2b969115b60127fdc10bbbbf02c46d864b280a1e7bb7c6; support0057aae1dc61cfcb69c43bd2fcb5b43674e66a252c409a857b71a069eef82980. Every entry in author manifest matches, including proposal and distinct review (input-checks.json). Copied three modules and environment pins were checked before replay. uv preflight installed the isolated D environment and confirmed Python3.12.11/SymPy1.14.0; no missing-pin/setup failure occurred.

fresh-replay.json SHA cbb643c5f3fb79f2df2af5d51744c56ae4908bd5aaba66b82bacfdf3ed0fcfc7 is byte-identical to frozen result.json, including all three module hashes. Strict UTF8 decoding, no CR and terminal LF pass. Replay review01 childrc0, empty stderr, no timeout,90second cap, elapsed0.944702seconds. review02 childrc2 (outer shell1) rejected the same existing output, whose bytes remain the frozen hash; elapsed0.581696seconds. All raw argv/streams/returncodes remain in logs. Both outer logger and child used uv run --project STAGE --frozen python; UV_CACHE_DIR=STAGE/uv-cache, UV_PROJECT_ENVIRONMENT=STAGE/.venv, UV_PYTHON_INSTALL_DIR=D:/uv-python. The established process-tree-aware logger was copied unchanged. No extra targeted run or prior CI replay occurred.

## Implementation alignment: PASS

Public solve validates exact integer graph labels/focus, n>=3, rational0<=l<=h<1, positive weights, no loops, connectedness, nonempty distinct absent allowed pairs; parallel present conductances merge. Rational conversion reuses the accepted exact utility, rejecting floats/booleans. M is reconstructed from L+J/n, with the zero-sum inverse identity checked. The degree vector enters the directed focus correction. Uniform source and independently mixed target use the displayed quadratic-over-linear coefficients; actual m+t is retained. There is no iid factor cancellation or call to exact_support.solve. Imports are limited to rational, compare, minimum, encode_certificate and InconclusiveError.

Endpoint stationary numerator [a1-r*a0,2*a2,r*a2] and quadratic normalized-objective cross product agree with the distinct accepted theorem. roots classifies identically zero, constant, linear, nonreal, repeated and two-real cases and retains only strictly positive roots. Boundary0 is always included. Endpoint oracles use the same physical allowed set and strength continuum; every tied endpoint/global physical edge is retained. add_action collapses zero to one no-action label. Per-edge list is not symmetry grouped, so no physical edge can be lost through an incorrect class key.

Candidate roots are algebraically deduplicated with exact compare. A balance already equal to an earlier stationary candidate retains that earlier correctly computed objective; adding its kind does not invalidate comparison. A newly inserted balance root has defining-polynomial equality by exact quadratic construction. Identically-zero cross products justify the same shortcut at all candidates. Else the sign of cross is correct because (1+r*t)*O0*O1 is positive. First-endpoint ties are consistent. Unresolved comparisons propagate InconclusiveError. No external evidence expression is executed; roots uses sympify only for internally supplied exact coefficients in solve/checker, not an advertised untrusted-expression API.

## Mathematical/source bridge: PASS by distinct reuse

The governing proposal e24ab919b620169d69b48b7fabae65e265598ad4a6d61da75bd69d025c3398f0 and distinct analytic review2dd02113766370e953ddce65ea3af535c83fca976814aa765cb27bde1e718d8e were read and hash-checked. That review supplies directed first-step identity, degree/rank-one expansion, positive uniform-component coercivity for h<1, attained positive same-action oracle, continuum endpoint reduction and strict quasiconvexity/candidate completeness. These are analytic dependencies, not proved by the finite checker. The implementation asserts a0,a2,r positivity consistently with them. It correctly uses uniqueness from strict quasiconvexity rather than claiming strict convexity: negative and zero curvature examples are retained. The source review's initial missing-SymPy setup failure and authorized recovery remain part of its unchanged historical record; this audit does not recast them as two successful mathematical runs.

## Specification and finite coverage: PASS

Thirty independent directed transition first-step comparisons cover all declared physical edges, strengths0,1/2,2 and both endpoints in four cases. Diagonal hitting is zero by omitted target row; each source average divides by n, then independent target weights are applied. This differs genuinely from reusing the solver's pseudoinverse objective. Three symbolic identities cover both derivatives and directed expansion. Seven abstract root classifications include zero/negative roots and seven malformed inputs reject. The weighted path retains negative-curvature99/100 and zero-curvature98/101 no-action cases, uncertain interval[0,99/100], and uniform collapsed four-cycle location ties. Abstract root tests are not claimed graph realizations.

Fresh-output protection is checked before computation and exclusive creation protects final writing. Exact support remains partial: dyadic enclosures/exact zero simplification may be inconclusive or expensive; serialization also can fail safely before opening output. No epsilon tie, all-input termination, complexity, novelty or real-workload benefit is asserted. Independent finite replay is not universal enumeration. All other stages/project remained read-only.
