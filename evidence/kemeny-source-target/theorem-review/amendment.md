# Authorized setup recovery

Root permits exactly one corrected invocation after the first launcher failed importing SymPy (ModuleNotFoundError). The missing pin-copy errors and raw rc1 are retained. Copy verified project/experiments/kemeny-workload-proof pins into this stage. Same frozen evaluator, graph and hypotheses; no mathematical expansion. Both uv layers, process-tree-aware 60-second limit. Two launcher attempts maximum, no further retry.
