# Portable checker follow-up

PASS, scoped to reproduction and implementation alignment of verify_certificate.py SHA256 7b656d33739a7a77834d1ae47f8115cd830f04bb05638a68a8de73189f747749.

Read the complete checker. Its sparse integer operations implement exact polynomial arithmetic correctly. OTHER-part first/second/third moments include each singleton's +p contribution. S0 and S1 are exactly the direct sums of q(n-q) and q(n-q)^2 over other parts. Its six source numerators and denominator complements agree with the independently reviewed source transcription. Each complement identity is tested exactly. Positive constants and positive coefficients prove the denominator factors positive on the domain. Adjacent transpositions correctly test full gap symmetry; degree is explicitly tested. The finite-support lemma remains supplied by the earlier mathematical review, as the checker explicitly states.

The fresh successful replay reconstructs the same complete coefficient dictionary as the fixed certificate, which my independent SymPy reconstruction already matched. Therefore equality to the SymPy result follows through that identical byte-pinned dictionary, not just matching headline counts. Fresh output confirms 1777 terms, 18 partitions, degree 5, constant 60948. No external package is imported. Exact certificate hash is checked before strict top-level fields, variable order, exponent arity/type/nonnegativity, coefficient type/positivity, and duplicate-exponent checks. The mutation probe increments one coefficient and is rejected with rc=1 before output creation; it tests the frozen-identity gate, not each downstream schema rejection in isolation.

Runtime was created by uv venv --python 3.12.11 in the review stage with D-drive cache. Fresh valid replay rc=0; modified-input rejection rc=1; runtime query confirms CPython 3.12.11. Actual argv, cwd, stdout, stderr and returncodes: portable-run-0.json through portable-run-3.json. Exact checker, certificate, Python executable, and successful result hashes: portable-hashes.json. The runtime hash is the venv python.exe launcher executable hash, not a hash of the complete standard library distribution.

This follow-up does not re-audit publication prose, novelty, practical impact, or matrix calculations. It extends the earlier frozen-certificate verdict to this exact portable implementation.
