# Symmetry optimization experiment log

All times use Asia/Calcutta host time. Commands are run from
`D:/CodexWorkspaces/mathematics-atlas/symmetry-work` unless a record says
otherwise. Raw console output and command metadata are retained as numbered
artifacts; failed attempts are never overwritten.

## 2026-09-08T06:03:57.9902209+05:30 — staging initialization

- Read the complete chain-of-evidence skill, its evidence contract, the frozen
  symmetry contract, the version-1 engine contract, project instructions,
  baseline source, tests, and frozen fixtures.
- Copied `project/src/atlas_engine`, `project/pyproject.toml`, and
  `project/uv.lock` into this isolated staging directory. The setup command
  compared SHA-256 hashes for all 11 baseline package files and every copy
  matched its source. Generated bytecode was removed from the staging copy.
- Began the selected serial implementation. The project checkout, fixtures,
  Site, Git state, and root-owned evidence ledger were not modified.

## Retained commands

| Attempt | Exit | Runtime seconds | Outcome |
|---|---:|---:|---|
| `uv-sync-attempt-01` | 0 | see command JSON | Created isolated `.venv`; installed the editable package and pytest with uv. |
| `pytest-attempt-01` | 0 | see command JSON | 30 tests passed. |
| `correctness-attempt-01` | 0 | see command JSON | 15,192 exact ordered-pair records; zero failures. |
| `benchmark-attempt-01` | 0 | see command JSON | 21 paired rounds; every output dictionary equal. |
| `pytest-attempt-02` | 1 | logger did not start | PowerShell parsed pytest `-p` as an ambiguous wrapper parameter; no test process started. |
| `pytest-attempt-03` | 0 | see command JSON | Retried with an explicit command array; 31 tests passed with the cache provider disabled. |
| `manifest-attempt-01` | 1 | see command JSON | Structural counts passed, but the export-only comparison treated one trailing blank line as substantive. |
| `manifest-attempt-02` | 0 | see command JSON | Baseline byte-identity and all raw record-count checks passed. |

The associated `*.command.json` files contain exact argv, timestamps, working
directory, measured wrapper runtime, and exit code. The `*.console.log` files
retain raw output. Attempt 02 is retained even though its failure occurred in
PowerShell argument binding before pytest started. Manifest attempt 01 is also
retained; only the manifest checker was corrected to ignore trailing newline
count when verifying the explicitly allowed package export edit.
