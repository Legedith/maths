# Preliminary correctness read

Read root's private novelty-paper-work/paper.md on10September2026. This is a preliminary mathematical/editorial read, not final integration, source-priority, or release certification. No evaluator or web call.

The copied six-term update, moment formulas including cubic +p, ranking identity/positive decomposition, parameter domain, ties and negative sign convention agree with the accepted audits. The correction formula and all displayed K90,10 fractions agree with my fresh independent exact review. The source-version-specific framing is appropriate. Restricting the conjecture claim explicitly to r>=3 avoids silently certifying the malformed equation13. No mathematical error was identified in these displayed results.

Two small changes requested before final integration:

1. In Section3, explicitly state that the polynomial in the abstract aggregates U,V,W has weighted degree at most5 with weights1,2,3 (as established in the accepted independent audit). The current wording reports the five-gap degree and then invokes a degree bound for arbitrary gap counts; the stable aggregate degree supplies the clean independent reason that unseen larger supports cannot occur. Suggested sentence: 'Before fixing the number of gaps, the expression has weighted aggregate degree at most five when U,V,W have weights1,2,3; therefore every F_s has total gap degree at most five.'

2. In Section7, say that output paths must be fresh/nonexistent. The shown exclusive-output commands can otherwise be mistaken for rerunnable overwrites. No additional evaluator is needed.

Optional clarity: Section3 uses q_j for the full partition after q_1,...,q_r previously denoted only non-singleton parts. The instruction to include singletons is sufficient, but writing the full size list as (q_1,...,q_r,1,...,1) makes n unambiguous.

Source-comparison and release-bundle links are future integration dependencies and were not certified by this read. The final independent review must check their actual promoted bytes and the scoped priority statement. The bridge P2 hash typo was separately corrected with prior manifest preserved, without mathematical changes.
