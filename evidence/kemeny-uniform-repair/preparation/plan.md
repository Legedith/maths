# Portable uniform-workload proof preparation

2026-09-08. Root owns this isolated stage; current PR and its final review
are unchanged. Reuse byte-pinned sparse integer polynomial arithmetic.
The new script reconstructs closed electrical score and trace expressions;
their graph interpretation requires the independent mathematical audit.
It never evaluates certificate expression strings.

Falsifiable checks: all three full ranking differences agree with their
complete frozen positive dictionaries; all three no-op differences agree
with their complete dictionaries; every reconstruction denominator has
positive coefficients and constant throughout its declared orthant.
Pin both input digests and output every dependency hash. One canonical
batch under uv/frozen Python 3.12.11 with a 90-second cap. Any correction
must preserve the original failed code/output and gets at most one further
bounded attempt. Independent review remains required before publication.
