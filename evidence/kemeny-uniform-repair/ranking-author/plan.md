# Frozen uniform-workload discovery
U(J)=n^-2 sum_ij E_i T_j with diagonal zero, same uniform distributions for every J=H+e. H=K_(a,b,c) join K1-uh,3<=a<=b,c. Hypotheses H1 uv always beats restore; H2 if a=b=c, uv is uniquely optimal orbit; H3 if max(b,c)>a, a largest original part's internal pairs are exactly optimal orbits. H4 the trace-update expressions supplied by root agree with independent full hitting matrices. No hypothesis presumed. Batch1 exact full matrices on (3,3,3),(3,3,4),(3,4,4),(3,4,5),(3,3,7),(4,4,5),(4,5,5), five missing-edge orbits. Derive Laplacian pseudoinverse action and compare score formula with full hits for these tuples. Batch2 only after separate freeze: symbolic compare uv/restore and uv/largest, covering ties and integer unequal gaps, retain all certificate failures. At most two batches. No source/algorithm novelty or runtime/real-world claim. Own stage only; current release unchanged.
