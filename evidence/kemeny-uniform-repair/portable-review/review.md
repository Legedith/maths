# Portable uniform checker: independent review

All four scoped gates PASS. No implementation correction required. Two review rounds; the second corrected only a reviewer invocation mistake, explicitly retained in correction.md and reject-* logs. Shared sources/project were read-only.

Implementation SHA256 08d338487dda30c3e7abcae09dc70b644073adb348762f5419dc17c1eb0f4302. Fresh replay SHA256 3bad57883dbd0ebbf5db99eae5f38f47b2f518f0ef15bafa6fdcdce916a1f631 equals root check-01.json byte-for-byte. helper-hashes.json proves copied helpers equal existing project files, reusing their accepted arithmetic audit.

- Reproduction PASS: valid-argv.json specifies uv run --frozen with stage-local project/environment, copied pyproject/uv.lock/.python-version. valid-stderr confirms CPython3.12.11 installed on D and stage-local .venv created; valid-rc=0. All six identities pass. One-byte appended whitespace retains valid certificate JSON but changes pinned bytes: corrected reject2-argv run returns rc1 with explicit changed-frozen-certificate ValueError and no output file. The earlier malformed rejection returned argparse rc2 and is not counted as certificate evidence.
- Specification compliance PASS: accepted integer domain and six orthants are unchanged. Ranking includes restoration; no-op comparison uses (m+1)S-T. Exact optimum set, ties and B/C swap rely on previously accepted independent mathematical proofs. The proof_boundary correctly states this dependency and excludes weighted optimum, nonuniform workload, physical performance and novelty. No numerical threshold theorem is certified by this script.
- Source verification PASS, reused: independent source report ../astra-fixed-workload-source-work/report.md and mathematical reviews ../astra-uniform-repair-review-work/review.md, ../astra-uniform-noop-review-work/review.md establish commute factors, graph counting, electrical reduction and orbit/domain arguments. Chandra Theorem2.1 and Monnig–Meyer Theorem2/Section8.1 concern known generic machinery; no family priority inference is made.
- Implementation alignment PASS: graph_expressions is the accepted formula in a common integer-polynomial denominator. Write d=da, k=k0/(nd), W=w0/(n^2d^2). Restore W/k=w0/(ndk0). Clearing denominators in the accepted Suv gives (2k0^2+2nk0+w0)/[d k0((d+2)k0+n)], exactly lines33–34. Trace denominator nd db dc k0 yields five numerator summands: 3d db dc k0, (a-1)n db dc k0, (b-1)nd dc k0, (c-1)nd db k0, w0 db dc. These match term by term. m counts the three cross-part blocks and hub spokes minus one. All reconstructed denominators and certificate numerator/denominator constants and coefficients are tested positive. Cross-products compare complete polynomials, not sampled values or author expression strings.

read_pinned checks exact accepted SHA256 values before parsing. Comparison sets are exact. coefficient_list rejects invalid exponent arity, negative/noninteger exponents, duplicate terms and nonpositive/nondecimal coefficients. positive requires a strictly positive constant, ensuring strict positivity also at all orthant boundaries. Exact pins fix other unused author metadata; the checker does not pretend to verify prose by reading it.

Prior proof boundary remains: n>=10 here, connected H, unit insertion, independent uniform source/target and zero self-hit; insertion optimum strictly improves the U-only objective against no action, without assigning monetary costs. Future copied release integration and final prose need separate verification; this verdict covers these exact script/helper/certificate bytes.
