# Frozen growth interpretation exploration

H1: for target outside inserted pair, linear hitting growth is positive exactly when source reaches that pair in G with target deleted; target in pair or source=target gives zero coefficient.
H2: averaging over a fixed source law and targets with a positive uniform component gives coercivity exactly when at least one supported source/target pair has positive growth.
H3: n>=4 alone does not restore coercivity for arbitrary source laws.

Two declared unit-weight trees only, vertices 0..3, inserted absent pair (0,2): star with center1 and source3; path0-1-2-3 with source1. Evaluate all targets by exact grounded first-step systems and inspect linear limits. At most two 60-second uv/logger invocations, first both graphs; no additional graphs. Pins verified from project workload experiment. All writes local, independent audit required.
