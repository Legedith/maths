# Independent mathematical review

Reviewer is a different agent from the postfailure proposal author. The author's solver was read but never executed/imported or copied. Independent evaluator review.py uses explicit singleton insertion endpoints, and the full determinant det(tD+L), rather than merged adjacent pairs and principal-minor coefficient sums. No verdict from the reviewer's unrelated all-deletion work is used as evidence here.

## Scope and orbit completeness
The theorem domain is integer 3<=a<=b,c and H=(K_(a,b,c) join K1)-uh. Missing edges consist precisely of the one removed cross-part edge uh and pairs within each original independent part. Within A, the pair either contains u or does not. These give exactly the five listed categories. Permutations within A except u, within B, and within C act transitively in the respective categories, preserving H; B/C categories may merge if b=c but keeping them separate preserves coverage. At a=3 an untouched A pair still exists (the two vertices other than u). All five categories therefore exist. Restoration is a SINGLE edge; strict domination of all four other categories implies a unique edge optimum, even when some losing categories tie (for example B and C at b=c). The theorem does not assert a complete ordering among the four losing alternatives.

## Spectral and normalization review
For the independent construction, every insertion endpoint is a singleton and every residual cell is an independent twin set. Its supported sum-zero vectors have transition eigenvalue zero; their direct sum with the cell-constant vectors spans the full vertex space. Thus K(full)=n-m+q2/q1 with q1,q2 the t,t^2 coefficients of det(tD+L). In the author construction, a merged inserted adjacent pair instead contributes eigenvalue -1/d; replacing the otherwise counted 1 by d/(d+1) subtracts 1/(d+1), precisely the author's correction.

At a=3 the untouched-A residual cell is empty. Its quotient column is zero, so a virtual transition eigenvalue zero is introduced; increasing m by one subtracts exactly the same unit from n-m. This justifies the formal quotient at the boundary. The independent full matrix check at (3,3,3) explicitly exercises this boundary.

For each connected undirected graph, P is reversible, 1 is a simple eigenvalue, and the other normalized Laplacian eigenvalues are positive. Factoring det(tD+L) gives q2/q1 as their reciprocal sum on the quotient. The full check uses trace((I-P+1*pi)^(-1))-1 with pi=degree/volume. This is the stationary-target Kemeny convention with zero time when starting at the target; it is not the convention with an additional unit or a uniform target.

## Source and execution integrity
All 20 hashes in the author's frozen hashes.json matched their current files (author-hash-check.json). Author batch1 rc=1, serialization retry rc=0, and batch2 rc=0 are retained and accurately disclosed in proposal.md. The reviewer does not reinterpret the failed first attempt as a success. Independent reproduction uses one frozen review round and records five child argv/runtimes/return codes plus separate stdout/stderr files; final review.log and review.rc record the aggregate execution.

The mathematical source claims used here are independently derived from the frozen graph definition and elementary linear algebra above. The proposal's external literature attribution is not needed for this theorem and was not independently audited; no bibliographic-priority, novelty, or practical-impact claim is certified. Portable implementation and final project prose remain outside this stage.

## Mathematical gate verdicts: PASS (scoped)
- Reproduction PASS: all five independently reconstructed symbolic K values match; all five exact certificate ratios match with positive coefficients and constants; five full 10-vertex matrices agree at a=b=c=3; exact uv-minus-untouched_A gap is 17/17856. result.json and round2.log record these checks.
- Specification compliance PASS: all five missing-edge categories exist and exhaust possibilities; b=c ties among alternatives do not defeat unique restoration because every alternate category is strictly worse; a=3 is included. No claim is made about the best non-restoration choice versus B/C or other failure types.
- Source verification PASS for mathematical entailment: graph operations, spectral decomposition, degree-based stationary distribution and determinant factorization are independently derived and verified against exact full matrices; the complete coefficient certificates support the signs. External literature attribution and novelty are excluded from this scoped gate.
- Implementation alignment PASS: independently reconstructed singleton quotient/full determinant agrees with author's merged-pair formulas. The new portable checker also splits endpoints, includes the correct -m term in K-n, correctly removes uh only when not restoring, inserts only singleton pairs, constructs all five partitions including C independently, pins all five comparison names and certificate bytes, and verifies exact cross-products. Unchanged arithmetic/parser audit was reused as directed, not repeated.

## Review-round accounting
Round 1's five independent determinant subprocesses each exited 0 and their outputs were retained. Aggregate round 1 was explicitly terminated (review.rc=-1) after completing the first two certificate checks because substitution into uncanceled differences caused unnecessarily large intermediates. Round 2 was frozen before execution, reused the independently generated expressions, and canceled the SAME differences before substitution. It completed all originally declared checks with rc=0. No new case, grid or method was added; both logs are retained.

## Portable replay
The fresh portable replay used uv --frozen, Python 3.12.11, zero package dependencies and a stage-local environment/cache/output. portable-argv.json, portable.stdout, portable.stderr and portable-execution.json retain its inputs, streams, runtime and rc=0. Stderr contains uv's first-environment setup messages, not an evaluator failure. portable-result.json exactly matches the root canonical result, including implementation and certificate hashes, and all five comparisons pass. portable-comparison.json records that equality. The mathematical/prototype and changed portable implementation gates pass; final project prose and assembled evidence bundle have not been reviewed in this stage. No publication novelty or practical benefit is certified.
