# Frozen independent evaluator, round 1
Reconstruct each of restore, uv, untouched_A, B, C using SINGLETON insertion endpoints (no merged-pair correction). Compute FULL polynomial det(tD+L), not author principal-minor solver, and extract t,t^2. Each candidate subprocess cap 300s. Verify all five symbolic K values and all five differences against saved author JSON by exact rational identity. Reconstitute every numerator/denominator certificate, compare exact polynomial identity after specified substitution, require all positive integer coefficients and positive constants. Independently build full matrices for all five interventions at (3,3,3) and check stationary fundamental-matrix K, including uv-untouched_A=17/17856. No grid. Maximum two review rounds; failed attempts retained.
Command: uv run --python .venv/Scripts/python.exe --no-project review.py
Author source is read-only and never executed/imported. Inputs proposal.md,batch2.json,batch2.py, hashes and logs are inspected. This evaluator uses a different partition and full determinant algorithm.
