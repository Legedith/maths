# Portable implementation review: PASS

Scope: implementation alignment and fresh reproduction only; reuse report.md for the mathematical theorem. This is the first release review round, with final publication prose still pending. The sealed mathematical review artifacts were not modified.

Read the entire selected checker, release contract, and Python project declaration. The exact selected checker SHA256 is ef5d3b84e9f78ac3e6e4840a3be675c2806f3df7aa89efa1ef4d5b776dcc4ffd. Its quotient neighbor matrix agrees entry-for-entry with the independently proved graph reduction. Sizes/degrees are positive and weighted cell balance is checked exactly. The t^0 determinant coefficients vanish, and both t^1 coefficient polynomials have positive coefficients and constants, independently certifying positive ratio denominators.

The subset determinant recurrence is correct: mask identifies columns used in the first popcount(mask) rows. Adding column j creates precisely one inversion for each previous column greater than j, counted by popcount(mask>>(j+1)). Every full permutation occurs once. Numeric mask order is topological because adding any previously absent bit increases the mask. All masks are initialized through predecessor transitions even when their polynomial value vanishes, so partial[mask] is defined. The recurrence therefore computes the Leibniz determinant grouped by used columns.

Arithmetic modulo t^3 is sufficient and exact: all t exponents are nonnegative, and projection modulo t^3 is a ring homomorphism. Discarded degrees cannot contribute back to t or t^2. t_coefficient correctly removes the selected t exponent, preserving x,y,z exponents, without collisions. Products of the extracted coefficients are then ordinary three-variable products because their t exponents are zero. The computed before_second*after_first-after_second*before_first is exactly -N with the previously proved sign convention.

Full equality to the frozen coefficient dictionary connects this implementation to the independent SymPy full-permutation determinant review, not merely to summary counts. Exact SHA256 rejects any changed input before parsing; expression strings elsewhere in the author packet are never executed. Coefficient parsing verifies exponent arity/types, positive integer decimal strings, and duplicate exponents. This is intentionally a fixed-input checker, not a general author-packet schema validator. That scope is accurately stated.

Fresh uv run --project [selected project] --frozen python [checker] used the project's pinned CPython 3.12.11 and dependency-free lock. UV_PROJECT_ENVIRONMENT directed creation into the review stage, preserving shared files; UV_CACHE_DIR is on D:. The successful replay has rc=0 and exactly 284 terms, minimum 6, constant 1784916000, degree 10. One modified coefficient is rejected at the hash gate with rc=1 and no result file. Actual argv, cwd, relevant environment, stdout, stderr and rc are in portable-replay-0.json through portable-replay-2.json. The mutation checks frozen-identity rejection, not each parser guard independently.

portable-hashes.json pins the checker, certificate, pyproject, lock, Python pin, release contract, runtime executable and fresh result. Runtime query confirms 3.12.11; executable hash identifies the venv launcher, not the entire Python installation. This review certifies the exact portable implementation for the specified uh deletion and uv addition. It does not certify other damage orbits, novelty, practical benefit, or unreviewed final prose.
