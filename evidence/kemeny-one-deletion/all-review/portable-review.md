# Portable all-deletions alignment: PASS

Reviewed the complete new verify_all_deletions.py and reused the independently audited, unchanged arithmetic verify_certificate.py (SHA256 ef5d3b84e9f78ac3e6e4840a3be675c2806f3df7aa89efa1ef4d5b776dcc4ffd). The new checker SHA256 is 4ebde785ce3515a0d4d5284b71f5e04a5a536197dc315cae60d293435682b2f9. This review extends the completed six-case mathematical audit to this exact implementation; final publication prose remains separate.

Its six case endpoint pairs and residual-cell sizes agree with the graph/orbit construction reviewed in manual-proof-notes.md. In particular A=a-3=x is permitted for rB/rh, whereas every other cell has strictly positive size. Allowing a zero constant in the cell-size coefficient test is therefore correct for these explicit constructions; degrees still have strictly positive coefficients and constants. The formal zero-column argument supplies the empty-cell spectral interpretation. Weighted cell balance, zero determinant constants and positive q1 factors are checked exactly. No extra zero-cell or degree-division assumption is introduced.

The reused subset determinant arithmetic preserves exactly the needed t and t^2 coefficients. The new cross product raw_negative_numerator*certificate_denominator=certificate_negative_numerator*raw_denominator has the correct sign and proves equality of rational functions throughout the domain, because both raw q1 factors and saved denominator are positive there. It does not rely on a computer algebra cancellation or execute any symbolic expression string. All six fixed certificate hashes and case labels are checked, and positive decimal coefficient parsing rejects malformed exponents, duplicates and nonpositive entries. The unchanged mathematical audit matched these same frozen dictionaries against independent full determinants, completing the source-to-runtime bridge.

Fresh uv --frozen replay with the project's pinned dependency-free CPython 3.12.11 project returned rc=0 and all six case identities/positivity checks true. A single modified uB numerator coefficient was rejected at frozen-input validation with rc=1 and no result file. Actual argv/cwd/environment/stdout/stderr/rc are preserved in portable-replay-0.json through portable-replay-2.json. UV_PROJECT_ENVIRONMENT points to a new review-stage .portable-venv; D-drive cache used; shared project files unchanged. portable-hashes.json pins source, reused arithmetic, six inputs, runtime launcher, project files and fresh result. The mutation probe certifies hash rejection, not an exhaustive schema-fuzzing claim.

Scope matches the proven theorem: every single original-edge deletion for a fixed within-minimum-part uv in K_(a,b,c) join K1, including ties and a=3. No multiple-failure, optimality, publication-priority, real-world-benefit, or formal proof-assistant conclusion is certified.
