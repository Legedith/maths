# Portable action-choice certificate

Implementation worker /root/astra_general_proof_verifier. This packet requires a distinct independent reviewer; PASS output does not self-certify a final evidence gate.

From this directory, with an existing output parent and a fresh filename:

    uv run --frozen python verify_action_choice.py --output fresh-result.json

Python3.12.11 and SymPy1.14.0 are pinned by the byte-copied compatible pyproject.toml/uv.lock from project/experiments/kemeny-workload-proof. The retained project package name is an environment identity, not an additional dependency. The CLI needs no historical stage path or evidence JSON. It constructs expressions explicitly and never evaluates certificate expression strings. Use ordinary Python with assertions enabled. Both the precomputation path check and exclusive output creation reject existing files. Output is deterministic sorted JSON, explicit UTF8 LF, with actual script SHA256 and runtime versions.

The certificate reconstructs four equal-family switch/slope identities and full shifted numerator/denominator coefficients for five positive rational expressions: switch margin, restoration slope, negative incident slope, threshold and threshold complement. Positive constants include a=3. The accepted complete equal-family envelope and opposite-slope argument remain explicit analytic bridges.

For344 it directly reconstructs17 centered graph inverses and checks each Laplacian inverse identity: baseline and every16missing unit candidate. It emits every affine improvement margin and certifies all A-pair exclusions by exact affine endpoint signs, not sampled workload inference. Exact zeros and slope signs yield the complete action/no-action policy on[0,1); at theta1 all17 action labels have zero objective. Baseline volume50 and inserted volume50+t are retained.

For fixed344 theta14/405, the same baseline inverse reconstructs every r,s_theta,A,B. The code checks the general derivative and infinite-growth identity, all per-edge stationary identities and closed-form minima, positive r/B, and unit agreement with direct inverse results. Eighty exact rational bisections enclose each square root; full rational strength and minimum bounds are retained. Strict disjoint bounds prove every optimized B/C pair beats each competitor and no action, with the same unique positive strength4(sqrt(27945/22432)-1). The output also retains the exact half-strength improvement117691/11809800 and unit excess7429/11809800. Scalar calculus converting derivative signs and limits into globality is named as an analytic bridge; the checker is not a proof assistant.

One proof evaluation only: run.json records the60second cap, rc0, exact argv/cwd/D isolation and elapsed time; stdout.bin/stderr.bin retain actual bytes. Stderr contains successful uv environment/package setup. No failed proof attempt, retry or second batch. No shared repo, Git or PR11 changes. The manifest pins all inputs and compatible environment copies; hashes.json seals the local packet. Canonical result.json is frozen. Root integration must copy the checker bytes unchanged and use a fresh output, followed by independent code/replay review and its own canonical logging.

Scope: universal equal-family unit-versus-no-action theorem, the complete344 unit action policy, and the single344 fixed-workload variable-strength optimum. No universal unequal no-action classification, costs, real-network performance, generic-method novelty, Site update or public release approval.
