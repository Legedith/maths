# Independent strength-rescue review

All four scoped checks PASS. Reviewer /root/astra_damage_symbolic,2026-09-08. No material correction. Fixed344,theta14/405 only; excluded from PR11. No generic-method novelty, physical benefit or public implementation gate.

Reproduction PASS. One <=60s uv Python3.12.11/SymPy1.14.0 run reconstructs the baseline using the hub-grounded inverse and centering, independently of the author's full L+J/n inverse. It verifies LM=I-J/n and enumerates all16missing edges. Every r,s_theta,A,B,radicand,tstar,minimum and unit-minus-baseline field agrees exactly. Reconstructed m=50,Ttheta=12431/9720,common factor391/2430. All15author manifest hashes and the supplied report hash match. Exact rational bisection of square roots, independently of the author's integer-square-root construction, proves the twelve B/C minima strictly below every other class and baseline. Raw rational bounds and all winning edges are in result.json. No float comparison is used.

Specification compliance PASS. A={0,1,2},B={3,4,5,6},C={7,8,9,10},h=11 and only uh is deleted. Exactly one absent edge may receive t>=0; at t0 all action labels represent no action, not distinct optimal repairs. The fixed iid workload and zero self-hitting convention are unchanged as t varies. Actual objective is2(1-theta)/n times(m+t)[Ttheta-t*s_theta/(1+rt)], so the volume difference is retained. Edge-class counts2uv,1restore,1untouched_A,12B/C exhaust16. No randomization, strength cost, new graph/workload or no-action exclusion is introduced.

Implementation and analytic alignment PASS. Direct symbolic differentiation gives numerator A+B(2t+rt^2) over(1+rt)^2. Here every r>0,B>0. The general weighted positivity bridge is also sound: M is positive definite on1-perp; rM-Mvv^TM is PSD by Cauchy-Schwarz and has rank n-2 there, hence is nonzero for n>=3. Taking its trace against positive definite I+n*theta*e_h e_h^T gives B>0. This is the needed weighted argument, not an unexamined uniform-only substitution.

For A>=0 the derivative is positive at every t>0, so t0 uniquely minimizes that edge's objective. For A<0, its derivative numerator strictly increases on t>=0 and has one positive root. The stated radical solves that root; A<0 implies mr>1 and radicand>1 using B>0. The independently verified asymptotic f(t)/t -> B/r>0 rules out an optimum at infinity. Thus each class's stated minimum is global on[0,infinity), rather than a local stationary diagnostic. Exact rational square-root enclosures also show all three positive stationary strengths lie below1 in this instance.

The global optimum set is exactly all6B pairs and all6C pairs, each with the same unique positive strength4(sqrt(27945/22432)-1). The two displayed radical forms and displayed Ustar were independently simplified to equal the reconstructed objective. Strict minimum intervals exclude optimized restoration, both uv edges, untouched_A and no action; there is no additional edge or strength tie. Half-strength gives exactly U(1/2)-U(noaction)=-117691/11809800. No decimal approximation is needed for these conclusions.

Source verification PASS, scoped reuse. The distinct independent review ../astra-workload-noop-counter-review-work/review.md was read: it directly reconstructed all17unit/baseline matrices, certified the counterexample and no-action interval, and retained earlier failed attempts as failures. This reviewer authored the original unit counterexample, so that distinct audit, not this reviewer's own author verdict, is used for the premise. Current reconstruction independently checks all unit comparisons again. Accepted commute/covariance/rank-one methods and elementary scalar calculus are reused; no new literature claim is made. Nothing here clears family priority or measured network impact.

Execution: one successful reviewer run, no retry. stderr includes uv index/install messages, preserved without claiming empty stderr. Root author's two canonical runs remain as provided in the hash-pinned packet. plan.md,check.py,run.py,run.json,stdout.bin,stderr.bin,result.json retain this independent evaluation. input-hashes.json pins author and distinct prerequisite-review bytes; hashes.json seals the stage. Shared repository and all other stages stayed read-only. This is scoped next-round mathematical evidence, not current-release approval.
