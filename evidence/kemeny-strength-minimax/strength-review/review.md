# Independent continuum-strength minimax audit

Four scoped integrity checks: PASS. No correction required. This independent analytic/reproduction audit concerns a next-round candidate, not public implementation, CI, novelty, or current deterministic-unit release approval.

## Inputs and reproduction: PASS

Author proposal SHA2566e673f48ed1070017ab5cecd2ca17a245dbd844b658765508fc9f1cd5acd2ef7 and result SHA2569c1bb733c9312f6a8860be8130f1c74384a85fca2a715f135c6ad6a7cc0b0a43 are checked before execution. One reviewer run returned0 in4.921919584274292seconds under60s. No second run or failed attempt. Raw argv/cwd/rc/time are run.json, streams stdout.bin/stderr.bin. Stderr contains successful uv setup/index warnings, not proof failure. The launcher command was uv run --isolated --python3.12.11 python run.py with UV_CACHE_DIR set to this stage's uv-cache and UV_PYTHON_INSTALL_DIR=D:/uv-python; the recorded inner command uses uv --isolated with sympy1.14.0. These inherited D environment settings are recorded here because the reused raw logger does not serialize environment fields. No historical bytes were overwritten.

check.py independently constructs the344 graph by adjacency conditions, grounds at hub11, inverts the11x11 grounded Laplacian, centers it, and checks LM=I-J/n. It enumerates all16missing edges and all four exact classes:2incident,1restore,1untouched-A,12largest-B/C. It obtains m50,T0=985/792,T1=10657/7920, and verifies every author's r and s endpoint pair.

Independent radical enclosures use integer square roots on a2^200 dyadic lattice: k^2/2^400<=q<(k+1)^2/2^400. Exact rational interval addition, multiplication and division propagate these bounds; division rejects zero-containing denominators. This differs from the author's100-step bisection expression walker. All endpoint-oracle bounds and every stored candidate worst-regret bound contain the independent narrower enclosures. Eleven candidate entries across all classes were evaluated, including repeated zero-label representations, all positive stationary strengths and all positive balance points. All alternatives have lower bound strictly above the winner's upper bound. Result.json retains coefficients, stationary radicands, candidate bounds and all physical class labels.

Endpoint0 oracle consists of all12largest-part pairs; endpoint1 oracle is restoration. Their values agree with sqrt(112079)/33+5144/99 and sqrt(10081666)/250+162923/3000 via the independently enclosed exact minimum formula. The global minimax winner is only restoration(0,11) at its positive balancing strength. It strictly beats every endpoint-stationary candidate from every class, and t0. No float comparison or evaluation of author expression strings is used.

## Specification and continuum endpoint argument: PASS

Actions are every missing edge with a fixed t>=0 chosen before theta, including the single no-action graph represented by every t0label. Oracle can choose its own edge/strength separately at each theta. Workloads lie in a compact interval within[0,1), fixed iid hub mixture per comparison. Actual U=2(1-theta)f/n, f=(m+t)(T-t*s/(1+rt)); changing volume stays inside f and does not cancel across strengths.

For connected H, M is positive definite on1-perp, r>0,s_theta>0. Cauchy-Schwarz gives Q=rM-Mvv^TM positive semidefinite, kernel span(v) on1-perp and rank n-2>0. Thus B_theta=tr(Q)+n theta Q_hh>0 for theta>=0. This also directly avoids any ambiguity about taking a trace on the full versus restricted space. Compact theta and finitely many edges give a common positive lower bound for B_theta/r. The identity f=(m+t)[B_theta/r+s_theta/(r(1+rt))] gives positivity and a uniform linear lower bound at infinity. Consequently both oracle and per-edge minimax are attained, with no infinity minimizer, and the oracle is uniformly positive. The bounded-away-from-zero1-theta factor preserves these statements for actual U.

For any fixed chosen edge/strength its reduced objective is positive affine in theta. Regret plus one equals the supremum over all oracle actions of their pairwise ratios, even though that set is infinite. Each ratio is bounded by the larger endpoint value, and sup_j max{a_j,b_j}=max{sup_j a_j,sup_j b_j}. Therefore the chosen action's worst regret equals its endpoint maximum. Positivity and attainment make all ratios well-defined. This extension is analytically justified, not inferred from the finite-action source lemma alone.

## Candidate completeness and ties: PASS

The derivative is[A+B(2t+rt^2)]/(1+rt)^2, A=T-ms. Because B,r>0 its numerator strictly increases for t>=0. If A>=0, t0 is the unique endpoint minimizer, including A=0; otherwise the unique positive stationary point has radicand s(mr-1)/B>1. The minimum formula follows by q=1+rt: f=[Bq+B(mr-1)+s+s(mr-1)/q]/r^2. Hence finite exact endpoint-oracle comparisons suffice.

For a fixed edge the two normalized functions have the common positive(m+t)/(1+rt)factor. Equality reduces to the stated linear equation in t, with at most one positive crossing unless both coefficients vanish. Zero denominator/nonzero numerator gives no crossing, both zero gives identical functions, t0crossing is already included, negative crossings are inadmissible. An interior minimizer away from equality must be stationary for its locally unique active function; at equality it is the balance candidate. Coercivity supplies attainment and t0 is the only finite boundary. Identical endpoint functions reduce to one strictly unimodal function, including l=h. Therefore every minimizing strength is included, not merely one optimum. No omitted interval of minima exists. Across edges equal values retain all labels; all t0labels are one no-action intervention. The witness's strict comparison leaves a single positive restoration action/strength, so no ties are lost there.

## Source and implementation alignment: PASS

Distinct accepted endpoint review SHA256c0b0112767bb251a87c45eefccc12dd803f69ca7a0ca1e61f7774a7899a274d8 and strength-rescue review SHA25631d4cbaa231bf3002d6d92110d8051500fb82d43e6992145374f035b8d98b643 match the pinned source bytes. They support the unchanged covariance/strength and finite-affine ingredients. The new continuum-supremum/coercivity and candidate-completeness steps were independently checked above rather than attributed wholesale to those earlier reviews.

The author's interval walker uses exact rational arithmetic, enclosing root bisection and safe reciprocal bounds; its sign routine rejects unresolved nonzero signs. The candidate max interval is computed by taking maxima of lower and upper endpoints, which encloses the true maximum without choosing a floating branch. Its only float is a final display. The finite code does not implement every generic degenerate crossing case, but it asserts safe signs for the sole declared instance; the proposal presents those generic cases analytically and does not claim a general-purpose solver. This is an explicit scope boundary, not a missing mathematical case.

The author reports one successful original run and no failures; the raw original record remains unchanged. Our audit likewise has one successful run and no expanded graph/interval. The result is a generic elementary characterization plus one family-specific finite witness, not a universal restoration minimax theorem, real-network benefit, or priority claim. All shared stages stayed read-only.
