# Independent exact strength-minimax implementation audit

Four scoped integrity checks: PASS. No material correction required. This reviewer did not author either portable module. This attestation covers the documented exact solver/checker interface with assertions enabled, not final release, hosted CI, source-priority or physical-impact approval.

## Reproduction: PASS

Frozen solver SHA25611152f62717a691450361c4227a70eb7674e65b1c3bb3d5889464956898ad1a9; checker SHA2564b648d90676f564b2ae12aa39b0ee1be7144f547e39bda3671b3f1813d22ae01; result SHA256734c9f73753f2ab07232ab8dbf2a1583b42565e88623f9b50d7779aba904665b. All target and input-manifest hashes match, including contract9f748cbf4cb593b6128cd86f240350a8fa52396b066e0ef9fcc55239da25db10. Both modules, pyproject and lock were byte-copied to this isolated stage.

One fresh90s-capped uv --frozen replay returned0 and reproduced the complete author JSON byte for byte. The second invocation returned expected2 for an existing output before computation, leaving it unchanged. run1.json/run2.json retain exact argv,cwd,D environment,assertion setting,rc and time; raw streams are separate. No additional targeted evaluation or graph grid was necessary. Output is strict UTF8 with no CR bytes and final LF; both implementation hashes and Python3.12.11/SymPy1.14.0 runtime fields match. No failed proof attempt occurred. The expected rejection is recorded as a rejection, not a second mathematical pass.

## Specification and API semantics: PASS

solve requires integer n>=3 and valid focus; sorted interval endpoints must be exact rationals in[0,1). It rejects Python/SymPy floats and booleans, nonpositive weights, invalid vertices, self-loops, disconnected graphs, empty allowed sets, existing allowed pairs and duplicate/reversed duplicate allowed pairs. Parallel present edges are merged exactly; connectivity is checked after merging. Exact rational strings use Rational conversion, not an evidence-expression parser. Container/iteration/resource requirements remain documented caller responsibilities.

The solver constructs its own Laplacian inverse, checks LM=I-J/n, and keeps m+t inside f and actual2(1-theta)f/n. Endpoint oracles have precisely the same allowed physical edges and t>=0 continuum. Exact grouping by(r,s_l,s_h) is valid because T,m are shared; for noncollapsed intervals these determine the full affine objective, and for collapsed intervals they determine everything required at that point. Every grouped physical edge is retained. Both endpoint oracle actions and global actions deduplicate t0 as edge=None once. Positive location ties are preserved separately.

Endpoint stationary minima handle A>=0 including equality as t0; A<0 uses a positive radical. Candidate sets include boundary0, positive endpoint stationary points and positive balance. Exact comparison merges duplicate strengths while preserving their generating labels. Balance correctly handles positive/negative/zero roots, zero-denominator distinct intercepts and coincident functions, including collapsed intervals. Exact per-class uniqueness is asserted after comparison using the accepted strict-convexity theorem, and finite global comparison retains all tied locations. No no-action exclusion or unit-only location theorem is silently imported.

## Interval arithmetic and implementation alignment: PASS

The dyadic root bounds are rigorous: floor(sqrt(floor(q*2^(2b))))/2^b brackets sqrt(q) with the next lattice point above. Rational leaves are exact; sums and four-corner products enclose both positive and negative ranges. Integer powers use endpoint extrema and include zero when an even power spans zero; negative powers first invert only a zero-free interval, reversing reciprocal endpoints correctly even for negative intervals. Square-root intervals reaching negative values and unsupported algebraic forms raise InconclusiveError. Zero rational leaves remain exact; an unnecessarily wide root upper endpoint is conservative, not a false sign.

compare accepts nonzero signs only from intervals strictly away from zero. It uses80/160/320bit refinement, then exact symbolic simplification; equality is accepted only as an exact zero expression. Further unresolved signs raise InconclusiveError. No epsilon or float-based choice appears. encode_certificate can still fail at its160bit enclosure after a solve comparison succeeded at higher precision; the checker catches that exception BEFORE opening output and exits3. This is explicitly documented safe partial serialization, not a false certified result. Symbolic simplification has no internal time cap; the external subprocess cap and stated lack of all-input termination guarantee are necessary limits.

No historical evidence strings are read or evaluated. The documented solve input is rational graph data, and encode_certificate receives its trusted SymPy expressions. Internal enclosure/compare call sympify on those expressions; this audit does not endorse feeding arbitrary untrusted expression strings directly to these internal helpers as a separate API. The CLI exposes only an output path and has no D-specific runtime dependency, network request or evidence loader. Exclusive creation and existing-path checks protect outputs; assertions-disabled execution is outside the documented invocation.

## Meaningful validation and source bridges: PASS

The replay confirms80direct grounded objective comparisons:34464,weighted path4,cycle8,noaction4. direct_checks rebuilds each physical candidate Laplacian at t0 and1/2, grounds the last vertex, and uses the corresponding covariance principal submatrix directly. This is valid because covariance annihilates constants, so centering the grounded inverse is unnecessary inside its trace. It compares actual volumes and both workload endpoints against the rank-one prediction for every physical edge, not merely one class representative.

The344 case retains16allowed edges,4classes and11candidate entries. Exact endpoint radicals agree with the accepted result; restoration alone at the balancing strength strictly beats every other candidate. The weighted path has r3/2 and oldvolume3; its exact solution is retained. The collapsed uniform cycle retains both missing diagonals and zero regret with coincident branches. Untouched-A-only344 at theta14/405 returns no action once and zero regret. Seven malformed-domain examples raise ValueError. Five balance examples cover all branch classifications and explicitly state they are abstract, not graph realizations. Three symbolic identities check first derivative, second derivative and asymptotic slope. These tests are bounded implementation validations, not universal enumeration.

Distinct damage mathematical review astra-general-strength-convexity-review-work/review.md SHA25669c937bc3765819ee8bd2cde5227588f5fde166da81b1e94dab4568a7527b006 was read and hash-checked. It independently establishes the sharp absent-pair bound/equality, PSD-rank positivity, uniform coercivity and attained oracle, continuum endpoint interchange, strict convexity and complete candidate characterization. Prior accepted strength witness review SHA2568479861a835b0686f02733b678f54070e69bbc50f47095f0d456c153dc8409e0 is reused unchanged. Those analytic premises are explicitly named in the checker output; finite validations are not substituted for them. The worker's own mathematical authorship is not used as final implementation certification.

All shared/worker artifacts remained read-only. No current release or Git state changed. Four gates certify this local implementation/replay under its documented rational-input and inconclusive-comparison limits only.
