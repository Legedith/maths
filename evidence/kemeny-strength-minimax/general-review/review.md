# Independent graph-general strength audit

Four scoped mathematical integrity checks PASS. No correction required. Proposal SHA2566ecc0d9aac9812c791998befc422b189e9d66662a002a889366125277c341f75 and claims SHA2562c0b9775c20fd5eead21a2eaac76267c98478c66f576d6c1568248dddcd6718f match; all author/dependency manifests match in input-checks.json. Reviewer is distinct from author general. Zero evaluators and zero source queries; this is an analytic audit, not a publication or priority gate.

## Sharp bound and complete equality

Dirichlet energy at unit endpoint voltage equals effective conductance, hence its minimum is1/r. The trial voltage1/2 elsewhere gives energy(d_i+d_j)/4 because there is no i-j edge. Minimization gives1/r <= trial energy, not the reverse inequality. The two endpoint incident-edge sets are disjoint and contained in the full edge set, so d_i+d_j<=m. Combining gives mr>=4. This argument uses undirected conductance once per edge, no loops, and nonadjacency. It does not transfer to strengthening an existing edge.

If mr=4, both inequalities must be equalities. Equality in degree sum forces every present edge to touch i or j. Strict positive conductance then excludes every edge entirely among the other vertices. Equality in the Dirichlet variational bound forces the trial to be the unique interior harmonic solution, since the graph is connected with fixed endpoint potentials. At an interior v, the harmonic residual is (c_vj-c_vi)/2, so the two incident conductances agree. Connectivity with no other edges forces both to be positive. Thus all and only weighted K_(2,n-2) networks with equal conductances along each two-edge branch qualify. Different branch conductances may differ. Conversely series conductance c/2 on each branch, parallel addition and m=2 sum c give1/r=m/4. The n3 equal-weight two-edge path is included. Parallel original edges may be merged as stated. The equality characterization is complete, not only sufficient.

## Convexity, positivity and attainment

On1-perp write K=M^(1/2)[rI-ww^T]M^(1/2), w=M^(1/2)v and ||w||^2=r. This proves K PSD with rank n-2. For n>=3, tr K>0; diagonal K_qq>=0 implies B_theta=tr K+n theta K_qq>=B_0>0 for the admitted nonnegative theta. Also z=Mv is nonzero, so s_theta>0. These arguments require no multipartite topology or universal focus vertex.

The exact decomposition f=(m+t)[B_theta/r+s_theta/(r(1+rt))] is valid, positive, and bounded below by(m+t)B_0/r. For a fixed graph and finite allowed edge set the smallest B_0/r is positive. With1-theta>=1-h>0 this yields uniform actual-objective coercivity and a positive lower bound. The t0 action supplies a finite upper bound, so the oracle is attained in a finite compact strength range. Each fixed-edge endpoint maximum is continuous and coercive and likewise attains its minimum. No varying-graph uniform constant is asserted.

Independent differentiation gives f''=2s_theta(mr-1)/(1+rt)^3. The proved mr>=4 makes this strictly positive, including equality mr=4. Dividing f_l,f_h by fixed positive endpoint oracle values preserves strict convexity. For t1!=t2 and0<lambda<1, choose an endpoint function active at lambda*t1+(1-lambda)*t2. Its strict convexity puts its value strictly below the same convex combination of its endpoint values, each bounded above by the endpoint maxima. Hence the finite maximum is strictly convex. This active-function proof explicitly establishes strictness; it is not an unsupported assertion that arbitrary suprema preserve strict convexity. Attainment plus strict convexity gives exactly one minimizing strength for each edge, including a possible t0. Across edges location ties remain possible; repeated t0labels represent one unchanged graph.

## Continuum oracle and finite characterization

The law is fixed iid uniform-plus-point-mass at any fixed vertex q, zero self hitting, same law for every intervention. The known commute/covariance and rank-one identities yield U=2(1-theta)f/n, retaining m+t inside f. Each fixed edge/strength has a positive affine reduced line in theta. Ratio to the continuum oracle is the supremum of pairwise ratios. Every pairwise positive affine ratio is bounded by its largest endpoint value; sup over actions commutes with maximum over the two endpoints. Equality is attained at an endpoint, even though the oracle action set is unbounded before the coercivity argument. Positivity prevents division by zero. The earlier finite-action lemma alone would not justify this extension, but the explicit supremum argument does.

With A=T-ms, f' numerator is A+B(2t+rt^2). Its derivative in t is2B(1+rt)>0. Thus if A>=0 the unique endpoint minimum is0 (also when A=0), otherwise the unique positive root is(sqrt(s(mr-1)/B)-1)/r. The radicand minus1 is -rA/B>0. Endpoint oracles are finite comparisons of those per-edge minima, including no action.

For a fixed edge, f_theta=(m+t)(T_theta+B_theta*t)/(1+rt). Equality f_l/O_l=f_h/O_h reduces to t*(B_l O_h-B_h O_l)=T_h O_l-T_l O_h. This verifies the balance formula and its signs. Nonzero numerator/zero denominator means no balance, both zero means identical normalized functions, and nonpositive balances are omitted except0already present. A nonboundary minimizer either has a uniquely active differentiable endpoint with zero derivative or both endpoints active, hence is the balance. Identical functions use their unique stationary or boundary minimum; l=h is included. Coercivity excludes infinity. Comparing0, positive stationary candidates and positive balance therefore covers every per-edge minimizer; strict convexity removes duplicate distinct optimal strengths. Finite edge comparison retains all location ties.

## Four gates and scope

Reproduction PASS by exact analytic reconstruction above; no numerical diagnostic is needed for these identities and inequalities. Specification compliance PASS for every finite connected positive weighted loopless undirected graph n>=3 with a finite nonempty allowed absent-edge set and0<=l<=h<1. No arbitrary non-iid demand, point-mass theta1, disconnected baseline, existing-edge reweighting, multiple additions, randomization, adaptive intervention or cost claim is inferred.

Source verification PASS only for unchanged accepted mathematical dependencies: distinct continuum-strength review SHA2568479861a835b0686f02733b678f54070e69bbc50f47095f0d456c153dc8409e0 was read; the strength-rescue review SHA25631d4cbaa231bf3002d6d92110d8051500fb82d43e6992145374f035b8d98b643 supplies its unchanged covariance/strength inputs. New graph-general Dirichlet/equality/convexity steps were proved independently here. Primary literature attribution and novelty assessment remain pending; this source gate does not clear them.

Implementation alignment PASS for this analytic proposal/claims packet: H1-H3 match the exact arguments, hypotheses and limitations, with zero author evaluators as declared. No general-purpose numerical solver is claimed. Hash checks were read-only; only this reviewer stage was written. No current release, hosted CI, physical benefit or publication priority is approved.
